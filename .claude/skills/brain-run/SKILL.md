---
name: brain-run
description: noteで作った記事を Brain（https://brain-market.com/）向けに拡張リライトして販売用ドラフトを自動生成するスキル。note/today/ の最新記事を読み、コード全文・プロンプト全文・テンプレ等の「買う価値」を追加して brain/today/ に出力する。投稿は手動。
allowed-tools: WebSearch, WebFetch, Write, Read, Glob, Grep, Bash, mcp__notion__notion-search, mcp__notion__notion-fetch, mcp__notion__notion-update-page, mcp__notion__notion-create-pages
---

# /brain-run

note で書いた記事を Brain 向けに拡張リライトし、販売ドラフトを自動生成するスキル。
note は「実体験の記録・入門・ダイジェスト（無料 or ¥500）」、Brain は「実践ノウハウ・コード全文・テンプレ付きの完成版（¥1,980〜）」という役割分担で運用する。

**投稿は手動**。Brain は公開 API なし・reCAPTCHA ありで自動投稿リスク高。ドラフト作成までが自動の現実的ライン。

---

## Phase 0: コンテキスト読み込み + 対象記事判定

### ① コンテキスト読み込み

**Brain 特化コンテキスト（最優先）:**
- `context/brain-profile.md` — Brain プロフィール（3パターン・文体ガイド・カテゴリ・タグ戦略）
- `context/brain-strategy.md` — Brain 運用戦略・商品ラインナップ計画・価格戦略・審査対策

**共通コンテキスト:**
- `context/note-profile.md` — プロフィール・ポジショニング
- `context/note-strategy.md` — 収益化戦略
- `context/voice-samples.md` — 文体サンプル
- `context/writing-rules.md` — 証拠優先ルール・AI禁止フレーズ
- `context/article-frameworks.md` — AIDA・Star Story Solution
- `context/existing-articles.md` — 過去記事一覧
- `context/content-memory.md` — 成功パターン（あれば）

**参照レポート（相場・競合分析の裏取り用）:**
- `D:/Claude/bussines/search/brain-competitors-2026-04-20.md` — 競合 10 人プロフィール・商品ラインナップ・サムネテンプレ
- `D:/Claude/bussines/search/brain-article-structure-2026-04-21.md` — Brain 売れ筋 8 商品の本文構成パターン・目次の作り方・無料/有料ゾーン設計・15 項目チェックリスト（**Phase 2 で必ず参照**）

### ② 対象 note 記事の特定

以下の優先順で対象記事を決定する：

**ルート A（自動）: 引数なし**
1. `note/today/` 配下に HTML ファイルがあればそれを対象
2. なければ `note/drafts/{sokuho,jituroku,knowhow}/` から最新更新日のファイルを選択
3. 見つからない場合はエラー終了（「Brain化できる note 記事が見つかりません。先に /note-run を実行してください」）

**ルート B（手動）: 引数で指定**
```
/brain-run {記事パス}
/brain-run latest         # 最新ドラフト（ルート A と同じ）
/brain-run url {note_url} # 既に投稿済み note 記事を URL で指定
```

### ③ 対象記事タイプと Brain 化可否判定

| 記事タイプ | Brain 化 | 拡張方針 |
|---|---|---|
| 実録（jituroku） | ◎ 最適 | コード全文・プロンプト全文・受注テンプレ・単価交渉スクリプトを追加 |
| ノウハウ（knowhow） | ◎ 最適 | テンプレ・チェックリスト・設定ファイル全文を追加 |
| 速報（sokuho） | △ 要検討 | 速報は鮮度が命で Brain の長期販売と相性悪い。対象外推奨 |

**速報記事を対象にしようとした場合:**
「速報記事は Brain 販売に向きません（鮮度依存のため）。それでも続けますか？」とユーザーに確認。

### ④ 同一記事の重複出品チェック

Notion「note記事管理」DB で対象記事の `Brainステータス` を確認:
- `投稿済み` → 「すでに Brain 出品済みです。リライト再生成しますか？」と確認
- `草稿完成` → 「すでに Brain 草稿があります。上書きしますか？」と確認
- `未作成` または該当フィールドなし → そのまま次 Phase へ

---

## Phase 1: Brain 相場調査

Brain 内で同ジャンルの売れ筋を調査し、価格・タイトル・説明文の相場を把握する。

### Step 1: Brain サイトで相場確認

**YOU MUST: 記憶ではなく WebFetch で実際に https://brain-market.com/ のランキングページを取得すること。**

1. https://brain-market.com/ の新着・ランキングを取得
2. 対象記事のトピック（Claude Code / AI副業 / クラウドワークス等）と類似する商品を 3〜5 件抽出
3. 各商品について記録:
   - タイトル
   - 価格（¥）
   - 販売数（購入済みの数字）
   - 説明文の冒頭 200 字
   - URL

**補助検索（WebSearch）:**
```
Brain "Claude Code" site:brain-market.com
Brain "AI副業" 人気 ranking
Brain "クラウドワークス" 販売
```

### Step 2: 相場まとめ出力

以下の形式で中間報告する:

```
📊 Brain 相場調査結果（同ジャンル上位 3〜5 件）

1. [タイトル] — ¥{価格} × {販売数}件 → 推定売上 ¥{価格×販売数×0.88}
2. ...

推奨価格帯: ¥{下限}〜¥{上限}
推奨説明文スタイル: {共通パターンの要約}
```

---

## Phase 2: Brain 用拡張リライト

note 版との差別化のため、「買う価値」を明確にするコンテンツを追加する。

### 絶対ルール

- **売れる構成 絶対ルール遵守（後述 A〜F）**: 見出し具体化（Brain 自動目次前提）・冒頭 3 点セット（ペイン→実績→ベネフィット）・無料ゾーン末尾フック・`<ul>/<ol>/<table>` 禁止（「・ 項目」段落と blockquote 連続で代替）の 4 大要素は Phase 5 の構成適合性スウィープで必ず検証される。違反したら自動 rewrite
- **note 版の完全コピー禁止**: 最低 30% 以上の追加・再構成を行う
- **個人体験談として明記**: タイトル・冒頭に「文系大学生が」「自分の場合」等を入れる
- **誇大表現禁止**: 後述のブラックリスト参照
- **実績証拠を盛り込む**: スクショ配置指示・具体的な金額・件数を残す
- **匿名化**: クライアント名・個人特定情報は伏せる

### 追加すべきコンテンツ（記事タイプ別）

**実録（jituroku）拡張:**

- note 版の導入・結果は流用
- 以下を追加:
  - クライアントとのやり取り全文（匿名化・Before/After 付き）
  - 単価交渉時のメッセージテンプレ
  - Claude Code に投げた実際のプロンプト全文
  - 生成されたコード全文（動作確認済み版）
  - 納品チェックリスト
  - トラブルシュート集（自分が実際に踏んだエラーと解決手順）
  - 受注〜納品のスケジュール表（時間配分の数字）
  - この手法を他ジャンルに応用する方法

**ノウハウ（knowhow）拡張:**

- note 版の概要は流用
- 以下を追加:
  - 設定ファイル全文（`.claude/settings.json` 等）
  - プロンプトテンプレ集（コピペで使える状態）
  - カスタマイズ方法（初心者向け・中級者向け）
  - よくある失敗パターンと回避策
  - 自分の運用ログ（数週間分のデータ）
  - 応用展開のアイデア 3〜5 個

### Brain エディタ対応フォーマット（2026-04-21 実機確認済み）

出力は **HTML 形式と Markdown 形式の両方** を保存する。**Brain エディタへの投稿時は HTML 版をそのままペーストする**（note と同じフローで装飾が自動反映される。Markdown記法のペーストでは装飾変換されない）。Markdown 版はバックアップ・編集・後続ツール連携用。

#### 使用可能な HTML タグ

- `<h2>` `<h3>` `<h4>` — 見出し（`<h1>` はタイトルフィールドに使うため本文では使わない）
- `<p>` — 段落
- `<b>` — 強調
- `<blockquote>` — 引用（ラベル付きは `<b>ラベル</b><br>本文` 形式・コロン禁止）
- `<pre><code>` — コードブロック
- `<hr>` — 区切り線
- `<a href="...">` — リンク
- `<code>` — インラインコード

#### 使用禁止 HTML タグ（Brain で崩れる・実機確認済み）

- `<ul><li>` / `<ol><li>` — **箇条書き・番号付きリスト禁止**（表示が崩れる）
- `<table><tr><td>` — **表禁止**（特に「同梱物一覧」「カリキュラム表」「比較表」等の構造化情報で致命的に崩れる）

#### リスト・テーブルの代替形式（YOU MUST FOLLOW）

Brain で崩れない以下の形式で書く。リスト構造や表構造の情報は全てこの 3 パターンに変換する。

**1. 短い箇条書き（ペイン・ベネフィット・おすすめ等）:**
```html
<p>・ 項目 1 の内容</p>
<p>・ 項目 2 の内容</p>
<p>・ 項目 3 の内容</p>
```

**2. 番号付き手順（チェックリスト・ステップバイステップ）:**
```html
<p><b>①</b> 手順 1 の内容</p>
<p><b>②</b> 手順 2 の内容</p>
<p><b>③</b> 手順 3 の内容</p>
```

**3. 構造化情報（同梱物リスト・カリキュラム表・比較表）:**
```html
<blockquote>
<b>ラベル 1（数量や種類）</b><br>
説明文 / 補足情報 / 属性（スラッシュや読点で区切る・コロン禁止）
</blockquote>

<blockquote>
<b>ラベル 2（数量や種類）</b><br>
説明文 / 補足情報 / 属性
</blockquote>
```

blockquote は Brain で「引用枠」として表示され、ラベル+本文の 2 行構造を保てる。テーブルの代替として最も実用的。

#### 使用可能な Markdown 記法（バックアップ用・Brain に直接は貼らない）

- `# ` `## ` `### ` — 見出し
- `**太字**` — 強調
- `> ` — 引用
- `` `コード` `` — インラインコード
- ` ``` ` — コードブロック（言語指定付き）
- `---` — 区切り線
- `[リンク](URL)` — ハイパーリンク
- **Markdown の `- ` / `1. ` リストと `| ... |` 表は使わない**（HTML 出力時に `<ul>/<ol>/<table>` に変換されて崩れるため。代わりに上記の代替形式を使う）

### 売れる構成 絶対ルール（YOU MUST FOLLOW）

Brain 売れ筋 8 商品（しゅうへい / リツト / こうめい / まな / トシノ×2 / メラメラくん / ユニコ）の本文構成リサーチ（`search/brain-article-structure-2026-04-21.md`）から抽出した必須ルール。違反したら Phase 5 の品質ゲートで即リジェクト扱い。

#### A. Brain サイドバー自動目次を活かす見出し設計

- Brain の商品ページは **H2 見出しから自動的にサイドバー目次を生成する**。本文内に別途「目次」節を置く必要はない（置くと二重になる）
- 重要なのは **H2 見出しを具体的なタイトルにすること**。自動目次の質を決めるのは見出しの言葉選びだけ
- **H2 / H3 共に章番号・識別子を一切付けない**。Brain が H2 / H3 それぞれに自動で番号を振るため、本文側で番号を書くと「11-1. Q1. 本当に文系・...」のような二重化が発生する。以下すべて禁止:
  - H2 の「第 1 章」「第 N 章」「Chapter 1」「1.」「2.」等（例: `## 第 1 章 プロフィール改修` → `## プロフィール改修`）
  - H3 の階層番号「1-1.」「1-2.」「2-1.」「B1-1.」「B2-3.」等（例: `### 1-1. ジャンル選定の失敗談` → `### ジャンル選定の失敗談`）
  - FAQ の「Q1.」「Q2.」等の質問番号と、回答行頭の「A.」（例: `### Q1. 文系でも使える？` → `### 文系でも使える？`、`A. 使えます。理由は...` → `使えます。理由は...`）
  - Bonus 章の「Bonus 1」「Bonus 2」や小見出しの「B1-1.」「B2-1.」等（例: `## Bonus 1 他ジャンル応用法` → `## 他ジャンル応用法`、`### B1-1. ライター案件で使う場合` → `### ライター案件で使う場合`）
  - ただし機能的識別子は例外として許可: 「Day1」「Day2」〜「Day N」（教科書型カリキュラムの順序ラベル）、「Week 1-2」「Week 3-4」等の期間表記（章タイトルの一部として残してよい）
- 売れてる 8 商品は全て H2 で構造を示しつつ、具体的な見出しを使っている。「第 1 章: 基礎」ではなく「なぜ○○は 9 割失敗するのか」等の具体タイトル

#### B. 冒頭は 4 点セット（順序固定）

全ての記事は冒頭 500〜1,800 文字以内で以下を順番で出し切る：

1. **冒頭 blockquote 宣言（2 段落必須）** — 「誰に・何を・どうなる」の要約（詳細はルール M 参照）
2. **ペインポイント提示（3〜6 項目の箇条書き）** — 「こんな悩みありませんか？」形式
3. **著者の実績（抽象化表現）** — 「Claude Code × クラウドワークスで提案資料・業務自動化系の案件を継続受注」のような抽象化実績（ルール K の審査回避規定に従い、**具体金額と期間の組み合わせを避ける**）
4. **この記事で得られるベネフィット（3〜8 項目）** — 購入後の未来を明示

目次は Brain 自動生成に任せる（本文内に置かない）。

**NG 順序（過去の失敗事例）**: 「この記事で手に入るもの（ベネフィットリスト）」から始めて、ペインも実績も出さない順序。これは売れない。

#### C. 見出しタイトルは具体性勝負

- ✅ 良い型: 「なぜ日本語 LP は 9 割崩れるのか」「9 割が挫折する本当の理由」「Day1: 環境構築で最初の感動体験」「【証拠】これが "創造神" の力」
- ❌ 避ける型: 「第 1 章: 基礎」「2. ツールの使い方」「3. 実践」「まとめ」

章番号 + 意味ある章タイトル、または Day 別（Day1〜Day7）、章-サブ章（1-1, 1-2）の階層を使う。「第○部」という部構成は調査 8 商品中ゼロ採用なので不要。

#### D. 章数は 6〜18 章

少なすぎる（5 章以下）と「薄い」と判断される。多すぎる（20 章以上）と読めない。教科書型は Day 別で 7 日カリキュラム等、実録型は章 + Bonus 4 本、ツールキット型は 15 項目 + スタイル 30 種のような同梱物リストで厚みを出す。

#### E. 無料ゾーン末尾のフック（6 型から必ず 1 つ採用）

有料ゾーン直前の「ここから先は ¥○,○○○」の直前に、必ず以下のどれかを置く：

1. **やること一覧見せ切り型**（教科書型向き）: 有料ゾーンの章タイトルを箇条書きで列挙し、「2 名に紹介すれば元が取れる計算」等の価格正当化を追加。しゅうへいの型
2. **ストーリークロージング型**（ブランド訴求型）: 「今日から、あなたは一人ではありません」等の情緒的クロージング。リツトの型
3. **第 1 章の冒頭 1 文型**（実録型向き）: 有料ゾーンの最初の 1 文だけを公開して「続きが気になる」状態を作る。こうめいの型
4. **共感クロージング型**（ストーリー型）: 「そんな時期がありました。でも今は〜」と著者の変化を再度提示。まなの型
5. **想い × 直接 CTA 型**（高単価向き）: 開発コスト・時間の提示で価値正当化 → 「今すぐ購入ボタンを押してください」の直接指示。ユニコの型
6. **今がタイミング宣言型**: 「今がその一歩を踏み出すタイミングです」等の決断促進。メラメラくんの型

フックなしで「ここから先は ¥○,○○○」に直行は NG。

#### F. 無料/有料ゾーンの分量比率

| タイプ | 無料ゾーン | 有料ゾーン |
|---|---|---|
| 教科書型（しゅうへい・リツト） | 全体の 20〜70%（実ノウハウの第 1〜2 章まで） | 残り |
| 実録型（こうめい・まな） | 全体の 50〜90%（LP 部分 + 第 1 章の冒頭 1 文） | 残り |
| ツールキット型（ユニコ） | 全体の 85%（LP 部分のみ） | 残り |

#### G. 段落細分化ルール（1段落1メッセージ）

- **1段落は60〜150字**。1段落に2〜3文まで。長い論理展開を1段落に詰め込まない
- **独立1行段落でリズムを作る**。印象的な一文・感情的な転換点・決定的な結論は、1段落1文で独立させる
  - 例: `<p>でも、眠れなかった。</p>` / `<p>それだけ。</p>` / `<p>5分で答えが返ってきた。</p>`
- 売れ筋の段落平均: 明鏡 150-250字、メアリ 23-100字。中間の60-150字がバランス良い
- **1段落400字以上は禁止**（読み飛ばされる・スマホで圧迫感）

#### H. 太字使用ルール（tiptap マーク継続バグ回避・絶対厳守）

Brain エディタは tiptap ベースで、**HTMLの段落中間に `<b>` / `<strong>` があると、段落全体を `<strong>` で包むバグ**がある（マーク継続バグ）。これを避けるため以下を厳守:

- **`<b>` / `<strong>` は段落全体を囲む形でのみ使用**: `<p><strong>全文</strong></p>` の形式のみ許可
- **段落中間・末尾の部分強調は禁止**: `<p>...続いて、<strong>重要な一文</strong>と続く</p>` はバグ発動で全段落太字化する
- 強調したい一文は**独立段落に切り出し**てから `<strong>` で包む（段落分割必須）
- 名詞句の太字（「情報じゃなくて視点の切り替え装置」等）は、独立段落化できないなら太字を諦める
- 太字の目安: **本文 1000 字あたり strong 7.0 以上**（明鏡 7.9/1000字 水準）。1 記事あたりの絶対数ではなく密度で判定する
  - 参考密度: メアリ 9.1 / 明鏡 7.9 / hulu35 2.0（薄い判定）
  - 教科書型 16k 字なら `strong` 120 個以上が目標
  - Phase 5 で段落生成後、自動付与ロジックで数字含む段落・結論フレーズ・決めフレーズに段落全体 strong を付与（詳細は Phase 5 参照）
- **確認コマンド**: `grep -nE '<p>[^<]{5,}<b>' {file}` で段落中間の `<b>` が検出されたら即修正（0件必須）

#### I. 話し言葉・読者への問いかけ（感情接点）

- 各H2セクションに**読者への問いかけを1回以上**: 「あなたも〜ですか？」「〜じゃないですか？」「〜したことありますか？」等
- 話し言葉を混ぜる: 「〜だった」「〜じゃないですか」「〜んです」「まあ」「正直」等
- AI感の出る定型フレーズ禁止: 「〜と言えるでしょう」「〜ではないでしょうか」「重要なのは」
- 1記事で「あなた」が10回以上出るのが目安

#### J. blockquote 使用制限

以下の5種類のみ blockquote で囲む。それ以外は地の文 or 独立段落で書く:

1. **プロンプト全文（単発・1個だけ使う場合のみ）** — ただし**プロンプト集のように連続させる場合（Day2 のプロンプト 20 本等）は blockquote ではなく `<pre><code>...</code></pre>` のコードブロック必須**。pre/code なら連続時の改行問題を回避でき、Brain でコピペボタンが付く
2. **テンプレ全文（単発）**（提案文・メッセージテンプレ） — 連続配置する場合は pre/code 化する
3. **Before/After 事例**（プロフィール改修の Before/After 等・連続 2 個まで）
4. **実際のクライアントメッセージ**（匿名化済み・連続 2 個まで）
5. **印象的な一言の単独強調**（「バイトの方が時給よくない？」のような独立セリフ・1記事に2〜3個まで）

**NG 使用**:
- **blockquote 連続 3 個以上は全面禁止**（2026-04-22 実機確認で、連続時に大きな改行マージンが挟まって読みにくいと確認。目次的な章リスト・同梱物一覧・カリキュラム表・用途リスト・案件タイプ別一覧等の構造化情報は全て `<p><b>タイトル</b></p>` + `<p>説明</p>` の段落ペアで代替）
- **プロンプト全文を連続 blockquote で列挙**（Day2 のプロンプト 20 本のような集合は `<p><b>プロンプト N タイトル</b></p>` + `<pre><code>プロンプト本文</code></pre>` の組み合わせで書く）
- 数値データの列挙（地の文化する）
- 箇条書きの代替（リスト禁止への対応として blockquote を乱用しない）

#### K. Brain 審査否認回避（情報商材フレーズ・緩和 B ルール）

2026-04-22 実機で即レス否認を受けた事例から抽出。Brain 審査は **テキスト自動判定**（即レスのため画像読み込みなし）で、ガイドラインの「短期間でお金持ちに」をうたうスキームに該当する記述を検知している模様。

**全面禁止（即否認候補）**:

| NG パターン | NG 例 | 修正例 |
|---|---|---|
| 具体金額 × 期間の組み合わせ | 「2 ヶ月で月 14 万円を達成」「3 ヶ月で 50 万」 | 「継続的に案件受注」「安定して案件を受けられる状態」 |
| 案件単価の個別表示 | 「¥65,000 + ¥45,000 受注」「¥140,000 実績」 | 「複数案件を受注」「提案資料案件を継続受注」 |
| 「ゼロから / ゼロの状態から」+ 実績 | 「副業ゼロから月 14 万」「プログラミング経験ゼロから 2 ヶ月で」 | 「プログラミング経験がほぼない状態から触り始めた」 |
| 「辿り着いた / 手に入れた」系の情報商材定番フレーズ | 「月 14 万まで辿り着いた」「成功を手に入れた」 | 「このレベルに到達した」「使える状態になった」 |
| 煽りキャラ設定 | 「ただの文系大学生」「素人の自分でも」 | 「非エンジニアの自分」「プログラミング初心者」 |
| 過剰効果訴求 | 「たった 30 秒で」「全部直る」「必ず稼げる」「絶対」 | 「30 秒で」「直る」「稼ぎやすくなる」「多くの場合」 |

**許可**（時短・手順・具体数は OK）:
- ✅ 「3 時間 → 15 分」（時短効果の具体数字）
- ✅ 「6 行 CSS」（手順の具体数）
- ✅ 「プロンプト 20 本」（同梱物の本数）
- ✅ 「7 日間」（カリキュラム期間）
- ✅ 「月 10 万円台で継続受注」（期間を伴わない概算表現）

**自動検知 regex（Phase 6 完了前チェック）**:
```
月\s*\d+\s*万       # 月14万・月10万
\d+\s*ヶ月.*月\s*\d+  # N ヶ月で月○○
¥\s*\d{1,3}[,，]\d{3} # ¥65,000・¥140,000
辿り着いた|手に入れ|ゼロから
たった\s*\d|全部(直る|できる|変わる)|必ず\s*(稼|成功|達成)
文系大学生|ただの\s*(文系|素人)
```
1 件でもマッチしたら Phase 6 完了前に修正必須。

#### L. タイトル規約

**タイトル NG 語彙**:
- `完全教科書` `完全攻略` `完全ガイド` `完全公開` — 「完全」系
- `○○付き` `○○プレゼント` `○大特典付き` — 物量訴求
- `バイブル` `聖典` — 権威訴求
- `月○万円` `○ヶ月で○万` — 金額訴求
- `稼げる` `儲かる` `必ず` `保証` — 確定成果訴求

**推奨タイトル型**:
- `{ツール名} 実戦メモ — {対象} が {期間} で触った記録`
- `{ツール名} 実録 — {実作業の具体内容}`
- `{ツール名} を {用途} で使った作業ログ`
- 実例: 「Claude Design 実戦メモ — 非エンジニアが 7 日で触った記録」

**自動検知 regex**:
```
完全(教科書|攻略|ガイド|公開|保証)
(\d+|○)大?特典
バイブル|聖典
月\s*\d+\s*万|\d+ヶ月で\d+万
稼げる|儲かる|必ず\s*(稼|成功)|保証
```

#### M. 冒頭 blockquote 必須（販売ページプレビュー訴求）

Brain の販売ページは**無料プレビュー部分の冒頭 300 字前後**が購入転換の鍵。冒頭 h2 の直前に `<blockquote>` を必ず配置する（ルール B の 1 番目）。

**構造（2 段落・段落全体 `<b>` で強調）**:

```html
<blockquote>
<p><b>{誰に} が {何を} に触り始めてから、{どうなる} になるまでの {期間} 分の {記録タイプ}。</b></p>
<p><b>{同梱物要約・許可範囲の数字含む}・{何までわかる} を、{書き起こしソース} から書き起こした。</b></p>
</blockquote>
```

**OK 実例**:
```html
<blockquote>
<p><b>非エンジニアが Claude Design に触り始めてから、提案資料・LP 制作案件で実際に使えるようになるまでの 7 日分の作業記録。</b></p>
<p><b>手順・プロンプト・日本語 LP の崩れ対策・案件応募時の使い方まで、実作業から書き起こした。</b></p>
</blockquote>
```

**NG 実例（審査引っかかり）**:
```html
<blockquote>
<p><b>文系大学生の非エンジニア副業者が、プログラミング経験ゼロから副業 2 ヶ月目で月 14 万円まで辿り着いた、Claude Design を副業ワークフローに組み込む 7 日間教科書。</b></p>
</blockquote>
```
→ `文系大学生`（ルール K）/ `プログラミング経験ゼロから`（ルール K）/ `2 ヶ月目で月 14 万円`（ルール K）/ `辿り着いた`（ルール K）/ `教科書`（ルール L）で 5 項目同時違反

---

### 記事タイプ判定ロジック

対象記事の**構成**と**同梱物**から以下のルールで 3 タイプから 1 つ選ぶ（タイトルキーワード依存は L のタイトル規約と矛盾するため撤廃）：

| 判定条件 | 採用テンプレ |
|---|---|
| Day 別 / Week 別 / Step 別の順序カリキュラム構成・メインコンテンツが「段階的スキル習得」 | **タイプ A: 教科書型** |
| 記事タイプが `jituroku`、または元ネタが「実際にやってみた・ケーススタディ・○ヶ月の体験」型 | **タイプ B: 実録型** |
| 大量同梱物あり（テンプレ 20 種以上・プロンプト集・GPTs 同梱）、または価格 ¥9,980 以上 | **タイプ C: ツールキット型** |

複数条件該当の場合は、優先度 C > A > B で選ぶ（高単価の訴求を優先）。

---

### 構成テンプレ タイプ A: 教科書型（Day 別カリキュラム）

代表例: しゅうへい「Claudeアプリの教科書」¥2,980、リツト「Antigravityの教科書」¥2,980

```markdown
# {タイトル}

<!-- 注意事項:
1. H2 見出しに章番号（「## 1.」等）を付けない（Brain サイドバー自動目次と二重化するため）
2. 本文内に「## 目次」節を置かない（Brain が H2 から自動生成するため）
3. <ul>/<ol>/<table> は Brain で崩れるため使用禁止。このテンプレ内の「・ 項目」は HTML 出力時に <p>・ 項目</p> として出し、<ul><li> には変換しない
4. blockquote 記法「> **ラベル**\n> 説明」は <blockquote><b>ラベル</b><br>説明</blockquote> として出す。表の代替としてこれを連続配置する
-->

## 購入者の口コミ

{発売後に追記。発売前はモニター感想 2〜3 名を入れるか「レビュー投稿後に追記予定」と明記}

## はじめに — こんな悩みありませんか？

### ペインポイント（3〜6 項目）

・ {ペイン 1}
・ {ペイン 2}
...

### 著者の実績

・ {文系大学生が 2 ヶ月で月 14 万、等の具体数字}
・ {Claude Code × CW で副業 ○件受注、等の実績}
・ {次の武器として○○に進出中、等の現在地}

## この教科書のゴール

{1 文で言い切る。例: 「7 日間で自分専用の AI ワークフローを完成させる」}

## 教科書の約束

・ 専門用語を使わない
・ コピペで使える
・ 失敗パターンを先に出す
・ できないことは「できない」と書く

## なぜ {ツール名} なのか

{競合との差別化ポイント。比較は blockquote 連続で書く（テーブル禁止）:}

> **{ツール A}**
> 特徴 / 価格 / 対応範囲 / 用途

> **{ツール B}**
> 特徴 / 価格 / 対応範囲 / 用途

## この教科書で手に入るもの

・ {ベネフィット 1}
・ {ベネフィット 2}
... （3〜8 項目）

## やらないこと / おすすめしない人

### やらないこと

・ {スコープ除外 1}
・ {スコープ除外 2}

### おすすめしない人

・ {属性 1}
・ {属性 2}

## 同梱物の全体像

{テーブル禁止。blockquote 連続で各項目を「ラベル + 説明」形式:}

> **{同梱物 1 の名前（数量付き）}**
> 説明文 / 内容

> **{同梱物 2 の名前（数量付き）}**
> 説明文 / 内容

...

## 教科書の全体像（Day1〜Day{N}）

{テーブル禁止。blockquote 連続で各 Day を「ラベル + 所要時間 + 到達目標」形式:}

> **Day1 {無料/有料} — {具体的な章タイトル}**
> 所要時間 ○分 / 到達目標 {到達目標}

> **Day2 {無料/有料} — {具体的な章タイトル}**
> 所要時間 ○分 / 到達目標 {到達目標}

...

## 著者プロフィール

{文系大学生 × Claude Code × CW × 2 ヶ月月 14 万の文章紹介}

・ {属性 1}
・ {属性 2}
・ {実績}

## Day1 {具体的な章タイトル}

（本文の最初の章。ここまで無料ゾーン）

---

<!-- 無料ゾーン末尾のフック: やること一覧見せ切り型 -->

## ここから先でやること一覧（買うとできること）

・ **Day2 {有料章タイトル}** — 説明
・ **Day3 {有料章タイトル}** — 説明
...
・ **特典① ...** — 説明
・ **特典② ...** — 説明

教科書は買い切りで ¥{価格}（月額不要）。
Brain 紹介機能で 50% 還元なので、{計算} で元が取れる計算です。

<!-- Brain エディタで「ここから先は ¥{価格}」境界を設定 -->

## Day2 {有料章タイトル}
（続く）
```

---

### 構成テンプレ タイプ B: 実録型（ケーススタディ）

代表例: こうめい「Brain 設計図」¥3,980、まな「11 ヶ月 100 万」¥22,980

```markdown
# {タイトル}

<!-- 注意事項:
1. H2 見出しに章番号（「## 1.」等）を付けない（Brain サイドバー自動目次と二重化するため）
2. 本文内に「## 目次」節を置かない（Brain が H2 から自動生成するため）
3. <ul>/<ol>/<table> は Brain で崩れるため使用禁止。「・ 項目」は <p>・ 項目</p>、blockquote 「> **ラベル**\n> 説明」は <blockquote><b>ラベル</b><br>説明</blockquote> として出す
-->

## 価格煽り（任意）

{先着○部限定・○部ごとに値上げ等、該当する場合のみ}

## こんな悩みありませんか？

・ {ペイン 1}: 例「1 記事書くのに 3 時間かかる」
・ {ペイン 2}: 例「下書きが 10 個以上溜まっていく」
・ {ペイン 3}: 例「売上がゼロのままなにも変わらない」
・ ... （3〜6 項目）

## 僕も同じ場所にいた

{著者の失敗談を 2〜3 段落。具体的エピソード}

## ターニングポイント

{気づきの瞬間を 1 段落で。「AI の使い方、間違ってたわ」等}

## 現在の実績

・ {実績 1}: 例「5 ヶ月連続月 100 万円」
・ {実績 2}: 例「7 ヶ月連続 6 桁」
・ {実績 3}: 例「実績者多数輩出」

## この記事で手に入ること

・ {ベネフィット 1}: 例「書けない→解放」
・ {ベネフィット 2}: 例「読まれない→卒業」
・ {ベネフィット 3}: 例「稼げない→抜け出す」
・ ...（4〜5 項目）

## このBrainの中身

{各章の章タイトル一覧をチラ見せ。各章 1〜2 行の概要付き。テーブル禁止、blockquote 連続で。章番号（「第 N 章」等）は付けない}

> **{章タイトル}**
> 概要 1〜2 行

> **{次の章タイトル}**
> 概要 1〜2 行

...

## モニター感想

{発売前のモニター 2〜3 名の感想を引用。blockquote でラベル付き形式:}

> **{モニター名 / 属性}**
> 感想の引用文

## おすすめな人 / おすすめしない人

### おすすめな人

・ {属性 1}
・ {属性 2}
...

### おすすめしない人

・ 楽して一瞬で稼ぎたい人
・ 手を動かす気がない人
...

## 価格・特典

・ 本編 ¥{価格}
・ 特典 1 ...
・ 特典 2 ...
・ 返金保証 {条件}

## FAQ

### {質問}

{回答}

{3〜5 問・各 Q は H3 見出しで質問文のみ、回答は通常段落で書く。Q1/Q2/A: のラベル一切禁止}

## 最後に

{伏線型クロージング。例「設計図は全部ある。でも○○だけでは△△にならなかった」}

---

<!-- 無料ゾーン末尾のフック: 第 1 章の冒頭 1 文型 -->

## {具体章タイトル}

{この章の冒頭 1 文だけを見せる}
例: 「84 部の Brain を企画するとき、僕が最初にやったことは ChatGPT に聞くことじゃない。」

<!-- Brain エディタで「ここから先は ¥{価格}」境界を設定 -->

（続く）
```

---

### 構成テンプレ タイプ C: ツールキット型（高単価・大量同梱物）

代表例: ユニコ「ClaudeCode × Remotion 教科書」¥29,800

```markdown
# {タイトル}

<!-- 注意事項:
1. H2 見出しに章番号（「## 1.」等）を付けない（Brain サイドバー自動目次と二重化するため）
2. 本文内に「## 目次」節を置かない（Brain が H2 から自動生成するため）
3. <ul>/<ol>/<table> は Brain で崩れるため使用禁止。「・ 項目」は <p>・ 項目</p>、番号付きは「① 項目」「② 項目」の段落、blockquote は <blockquote><b>ラベル</b><br>説明</blockquote> として出す
-->

## {キャッチコピー}

{ドラマティックな 1 行見出し}
例: 「AI が動画編集者の仕事まで奪った日」

## まだ {旧来の方法} で止まっていませんか？

{一段上の提案を 2〜3 段落で}

## 開発者としての権威付け

{著者が AI エージェント開発者・○時間開発・○円課金等の具体数字}

## 機能全部入り

{同梱物の網羅感を訴求}

## 同梱物詳細一覧

### テンプレート ○種類

・ **{テンプレ 1 の名前}** — {概要}
・ **{テンプレ 2 の名前}** — {概要}
...
{全○種類を全部列挙。量で価値を示す}

### プロンプト ○本

・ **{プロンプト 1 の名前}** — ...
...

### 動画 ○本

・ **{動画 1 タイトル}** — ○分
...

## 初心者でも使えます

{安心感の訴求。サポート体制の概要}

## サポート体制

・ Zoom サポート ○時間
・ 生放送 ○回
・ オープンチャット
・ 添削サービス

## マネタイズの可能性

{月○本で○円の収益等の具体試算}

## 購入者限定特典

・ 特典 1 ...
・ 特典 2 ...
...

## 費用感の説明

{月○本あたり○円、開発コストを考えたら激安等の価格正当化}

## このBrainへの想い

{開発コスト・時間の提示。「1 日 12 時間 × ○ヶ月 × ○万円課金」等で価値正当化}

---

<!-- 無料ゾーン末尾のフック: 想い × 直接 CTA 型 -->

## 今すぐ購入ボタンを押してください

{「このノウハウを受け取ってください」の直接指示}

<!-- Brain エディタで「ここから先は ¥{価格}」境界を設定 -->
```

---

### 分量の目安（タイプ別）

| タイプ | 無料ゾーン文字数 | 有料ゾーン文字数 | 全体章数 |
|---|---|---|---|
| タイプ A: 教科書型 | 3,000〜9,000 字 | 10,000〜20,000 字 | 10〜18 章 |
| タイプ B: 実録型 | 4,000〜8,000 字 | 8,000〜15,000 字 | 10〜15 章 |
| タイプ C: ツールキット型 | 8,000〜12,000 字 | 15,000〜30,000 字 | 15〜25 章 |

---

## Phase 3: タイトル・冒頭 blockquote の生成

Brain の商品ページには**「タイトル」欄のみ存在**（2026-04-22 実機確認）。購入前表示の訴求は**本文冒頭の blockquote（ルール M）**が担う。「サブタイトル」「商品説明文」欄は Brain に存在しないため、このフェーズで生成するのは以下の 2 つだけ。

### Step 1: タイトル A/B テスト

5 本生成してスコアリング。既存 Brain 売れ筋タイトルの型に寄せる。**ルール L のタイトル規約を必ず遵守**。

**Brain で効く（かつ審査通過する）タイトル型:**
- **実戦メモ型**: 「{ツール名} 実戦メモ — {対象} が {期間} で触った記録」
- **実録型**: 「{ツール名} 実録 — {実作業の具体内容}」
- **作業ログ型**: 「{ツール名} を {用途} で使った作業ログ」
- **Before/After 型**: 「{困りごと} の状態から {結果} になるまで」
- **具体ツール型**: 「{ツール名} × {他ツール} で {結果} を出した記録」

**タイトルスコアリング（各 20 点満点・合計 100 点）:**

| 項目 | 判定基準 |
|---|---|
| 具体性 | 固有名詞（ツール名）が入っているか（0-5点） |
| 結果明示 | 「何が書かれているか」が一目でわかるか（0-5点） |
| 審査規約適合 | ルール L のブラックリスト違反がないか（0-5点） |
| 検索キーワード | Brain 内検索で出てきそうなワードか（0-5点） |

**ルール L 違反タイトル（即リジェクト）:**
- `完全教科書` `完全攻略` `完全ガイド` `完全公開` `完全版` — 「完全」系
- `○○付き` `○大特典付き` `○○プレゼント` — 物量訴求
- `バイブル` `聖典` — 権威訴求
- `月○万円` `○ヶ月で○万` — 金額訴求（期間+金額の組み合わせ）
- `稼げる` `儲かる` `必ず` `確実に` `絶対` `保証` — 確定成果訴求
- `誰でも` `再現性 100%` — 絶対化
- `知らないと損` `絶対知るべき` — 脅し

### Step 2: 冒頭 blockquote 宣言文生成（ルール M 準拠）

販売ページの購入転換を決める冒頭 300 字前後のビジュアル要素。本文 HTML の先頭・h2 直前に `<blockquote>` で 2 段落を配置する。

**構造**:
```html
<blockquote>
<p><b>{誰に} が {何を} に触り始めてから、{どうなる} になるまでの {期間} 分の {記録タイプ}。</b></p>
<p><b>{同梱物要約}・{何までわかる} を、{書き起こしソース} から書き起こした。</b></p>
</blockquote>
```

**許可される数字要素**（ルール K の緩和 B 準拠）:
- 期間（7 日間・14 日間・7 日分）
- 手順数（プロンプト 20 本・6 行 CSS）
- 時短効果（3 時間 → 15 分・1/12）

**NG 要素**:
- `2 ヶ月で月 14 万円` 等の期間+金額組み合わせ
- `¥65,000 + ¥45,000` 等の案件単価
- `辿り着いた` `手に入れる` `ゼロから`
- `文系大学生` `ただの素人`

### タイトル・冒頭 blockquote の出力フォーマット

```
🏷️ タイトル候補:
1. [タイトルA] — 18/20点 ✅ 採用
2. [タイトルB] — 16/20点
3. [タイトルC] — 14/20点
4. [タイトルD] — 13/20点
5. [タイトルE] — 11/20点

💠 冒頭 blockquote 宣言文（本文 HTML 先頭に配置）:
{blockquote HTML コード全文}
```

---

## Phase 4: 価格・紹介還元率の提案

### 価格の決め方

Phase 1 の相場調査と以下の条件を組み合わせる:

| 条件 | 推奨価格 |
|---|---|
| Brain 販売実績ゼロ（初投稿） | ¥1,980 |
| 販売実績 1〜5 件 | ¥1,980〜¥2,980 |
| 販売実績 6 件以上 + スキ 10 以上 | ¥2,980〜¥4,980 |
| コード全文＋テンプレ＋設定ファイル付き | +¥1,000 上乗せ可 |
| 速報性がある（リリース 48 時間以内） | +¥500 上乗せ可（鮮度プレミア） |

### 紹介還元率の決め方

Brain の強みはアフィリエイト機能。還元率が高いほどアフィリエイターが拡散しやすい。

| フェーズ | 推奨還元率 |
|---|---|
| 販売初動（実績ゼロ〜5 件） | **50%**（拡散重視） |
| 安定期（6〜20 件） | 40% |
| 安定期（21 件〜） | 30%（利益重視） |

**出力フォーマット:**

```
💰 価格・還元率の提案

価格: ¥{価格}
理由: {根拠3行}

紹介還元率: {%}
理由: {根拠1行}

取り分シミュレーション（月5部販売の場合）
- 自己経由売上: ¥{価格} × 5 × 88% = ¥{金額}
- 紹介経由売上: ¥{価格} × 5 × (100% - {紹介率}% - 12%) = ¥{金額}
- 平均想定: ¥{中央値}/月
```

---

## Phase 5: 品質ゲート

### Step 0: 事実確認スウィープ（全件Web検索・必須）

**IMPORTANT: 記憶・学習データで判断することを禁止する。以下の全項目を必ず WebSearch / WebFetch で実際に調べてから判定すること。**

Brain は商品としての信頼性が命。誇張・誤情報・動かないプロンプトは即レビュー最悪化＆返金要求＆アカウント信頼失墜に直結する。

#### 対象クレームの抽出

記事全文を読んで、以下の種別に該当する「事実の主張」をすべて列挙する：

| 種別 | 例 |
|---|---|
| A. リリース日・バージョン | 「Claude Design は 2026-04-17 リリース」「v2.1.89 で追加」 |
| B. 機能名・コマンド名 | 「Claude Code の `/voice` コマンド」「MCP Elicitation」 |
| C. 機能の仕様・動作 | 「Claude Design は Pro/Max/Team プラン限定」「月 20 ドル」 |
| D. コマンド構文・設定値・URL | 「`claude.ai/design`」「`claude remote-control`」 |
| E. 外部サービスの仕様 | 「CW の報酬形態・手数料率」「Brain の還元率上限 50%」 |
| F. 統計・データ（自身の体験以外） | 「AI 副業市場の平均単価」等の一般的な数値 |
| G. プロンプト・コードの動作 | 「このプロンプトで 30 秒でスライド5枚が出る」「この CSS で日本語 LP が直る」 |

**対象外（事実確認不要）**: 自分の体験・Before/After 実測値（「3時間→15分になった」「95件応募した」等）

#### 検証フロー（種別ごと）

**A・B・C（Anthropic 関連）:**
```
1. WebSearch: "{機能名} anthropic release" または "{機能名} site:anthropic.com"
2. WebFetch: ヒットした公式URL（docs.anthropic.com / anthropic.com/news）
3. 記事の記述と照合。日付・バージョン・プラン条件は一字一句ズレがないか確認
```

**D（コマンド・URL）:**
```
1. WebFetch: https://docs.anthropic.com/en/docs/claude-code/overview（または該当ページ）
2. 実際のコマンド・URL・引数名と照合
3. URL にブラウザでアクセスできるか確認
```

**E（外部サービス）:**
```
1. WebFetch: 該当サービスの公式ヘルプ/規約/料金ページ
2. 記事の記述と照合（手数料率・プラン条件・利用規約の該当箇所）
```

**F（統計・データ）:**
```
1. WebSearch: 元ソースとなるデータを探す
2. 見つからない場合は記事から削除するか「〜との報告がある」等の留保表現に変更
```

**G（プロンプト・コードの動作）— Brain 特有の最重要チェック:**
```
1. 記事内のコピペ可能プロンプト・コード全文をすべて抽出
2. Claude API（または Claude Code ブラウザ版）で 1 本ずつ実際に実行
3. 記事で主張している挙動・出力・時間が再現できるか検証
4. 再現できなければ:
   - プロンプトを修正して再検証
   - 改善しないなら記事から削除（「動かないプロンプトを売る」は詐欺認定リスク）
5. 時間クレーム（「30秒で」「5分で」等）は自分のストップウォッチで実測
```

#### 判定と修正

各クレームについて以下を判定し、記録する：

```
✅ 検証済み: [クレーム内容] → ソース: [URL] または [実行結果ログ]
⚠️ 要修正: [クレーム内容] → 実際は [正しい情報] → 修正済み
❌ 削除: [クレーム内容] → ソース見つからず、または再現失敗 → 記事から削除
```

**修正ルール:**
- 間違いが発覚したら記事を即修正してから Step 1 に進む
- ソースが見つからない情報は削除するか「〜と言われている」等の留保表現に変える
- 動かないプロンプトは削除する（修正できない限り載せない）
- 「確認できなかった」は「記憶で判断して OK」ではない。削除か留保が必須
- **自分の記憶・学習データを根拠にしてはいけない。必ず WebSearch / WebFetch / 実行結果のいずれかが根拠として記録されること**

#### 速報記事の強化検証

記事が「最新機能の紹介」を含む場合は追加で以下を実行：
```
WebFetch: https://www.anthropic.com/news
WebFetch: https://docs.anthropic.com/en/release-notes/overview
→ 記事で言及している機能・日付・バージョンが実際に掲載されているか確認
```

#### 出力フォーマット（検証結果を本文執筆前に必ず出力）

```
📋 事実確認スウィープ結果

抽出クレーム数: {N} 個
  A. リリース日/バージョン: {n}
  B. 機能名/コマンド名: {n}
  C. 機能の仕様/動作: {n}
  D. コマンド構文/URL: {n}
  E. 外部サービス仕様: {n}
  F. 統計/データ: {n}
  G. プロンプト/コード動作: {n}

検証結果:
  ✅ 検証済み: {n} 件
  ⚠️ 要修正: {n} 件 → 修正完了
  ❌ 削除: {n} 件 → 記事から除外

検証完了日時: {タイムスタンプ}
```

---

### Step 1: 誇大表現スウィープ（必須）

記事全文を以下のブラックリストで grep し、該当があれば必ず修正する:

**即リジェクトワード:**
- 「誰でも」「必ず」「絶対」「確実に」
- 「再現性 100%」「世界初」「最強」「最速」
- 「保証」「収入保証」
- 「1日{数字}分で月{数字}万」系
- 「知らないと損」「知らないと危険」

**要注意ワード（文脈で判断）:**
- 「簡単に」「すぐに」「誰でもできる」 → 「個人的には〜」「自分の場合は〜」に言い換え
- 「稼げる」 → 「稼いだ（個人の体験）」に言い換え

**修正後は必ず「個人の体験談」タグが入っているか再確認。**

### Step 2: 構成適合性スウィープ（必須・Phase 2 の絶対ルール違反の検出）

Phase 2 の売れる構成 絶対ルール（A〜F）を記事に対して自動検証する。16 項目のチェックリストを適用し、欠落を rewrite で埋める。

**【見出し・構造】**
1. [ ] H2 見出しが具体的（Brain 自動目次の質を決める。「基礎」「実践」でなく「なぜ○○は 9 割失敗するのか」「Day1: 環境構築で最初の感動体験」）
2. [ ] H2 / H3 見出しに章番号・階層識別子を付けていない（Brain 自動目次と重複するため二重化する）。具体的には「第 N 章」「1.」「2.」「1-1.」「B1-1.」「Q1.」「A.」「Bonus N」等が一切含まれないこと。ただし「Day1」「Week 1-2」等の機能的識別子は例外
3. [ ] `<ul>/<ol>/<table>` を使っていない（Brain で崩れる。「・ 項目」段落、「① 項目」番号付き段落、`<p><b>ラベル</b></p>` + `<p>説明</p>` の段落ペアで代替）
4. [ ] 章数は 6〜18 章
5. [ ] 教科書型なら Day 別 or Chapter 別のカリキュラム（`<p><b>DayN タイトル</b></p>` + `<p>所要時間 / 到達目標</p>` の段落ペアで提示）がある

**【冒頭フック・ペイン提示】**
5. [ ] 冒頭 300〜500 文字以内でペインポイント 3〜6 項目を列挙
6. [ ] 著者の実績数字（部数・売上・期間）が冒頭 1,000 文字以内に出ている
7. [ ] ベネフィット 3〜8 項目を提示している

**【無料ゾーン設計】**
8. [ ] 無料ゾーン末尾に 6 型のどれかのフックがある（やること一覧 / ストーリークロージング / 第 1 章冒頭 1 文 / 共感クロージング / 想い × 直接 CTA / 今がタイミング宣言）
9. [ ] 有料ゾーン直前に価格の正当化フレーズがある（「動画 1 本あたり○円」「開発に○時間」等）
10. [ ] 購入ボタンのすぐ上に直接的な行動 CTA がある

**【社会的証明】**
11. [ ] 購入者の声が本文内（目次や冒頭節）に組み込まれている（発売前は「モニター感想」または将来追記枠を置く）
12. [ ] 販売実績（部数・売上）が本文内に明示されている（発売前は「発売後追記」と明記）
13. [ ] [UPDATE] ログを本文冒頭に用意（発売後、販売数・追加コンテンツの更新を随時追記）

**【コンテンツ価値の見せ方】**
14. [ ] 同梱物（テンプレ・動画・特典）の一覧が具体的な数量・内容付きで示されている
15. [ ] 「やらないこと」「おすすめしない人」リストがある

**【Brain 実機表示の最適化（2026-04-22 実機確認で追加）】**
16. [ ] **blockquote 連続 3 個以上の箇所が 0 件**（連続時に大きな改行マージンが挟まって読みにくい。同梱物一覧・カリキュラム表・用途リストは必ず `<p><b>タイトル</b></p>` + `<p>説明</p>` の段落ペアで代替）
17. [ ] **プロンプト全文（Claude への指示文・コピペ用）は `<pre><code>...</code></pre>` で囲まれている**（blockquote ではない。コードブロックならコピペボタンが付き、連続時の改行問題も回避できる）

**自動検証スクリプト（Brain エディタ内で javascript_tool で実行して 0 件確認）:**
```js
const nodes = [...document.querySelector('.tiptap.ProseMirror').children];
let run = 0, max = 0;
nodes.forEach(n => { if (n.tagName === 'BLOCKQUOTE') { run++; if (run > max) max = run; } else run = 0; });
// max >= 3 ならルール違反
```

**判定:**
- 14 項目以上 OK → 公開可
- 11〜13 項目 OK → 軽微修正で公開可（欠落項目のみピンポイント rewrite）
- 10 項目以下 OK → 自動 rewrite 実行（Phase 2 の該当タイプテンプレ A/B/C を参照しながら全体を再構成。最大 2 回、2 回後も基準未達ならユーザー報告）

**スウィープ実行方法:**
記事全文を読み、各項目を自動判定してチェックマークを付ける。欠落項目がある場合はその項目に対応するテンプレ（タイプ A/B/C）の該当セクションを参照して記事に追記・再配置する。特に「ベネフィットから始まっている（ペインなし）」「H2 に章番号が付いている」「`<ul>/<ol>/<table>` を使っている」「フックなし」の 4 大 NG は最優先で修正する。

### Step 2.5: 有料部分充実度ゲート（Brain 特有・必須）

Brain 記事の**最大の失敗原因**は「無料ゾーンは濃いが、有料ゾーンに入ると急に抽象的・薄っぺらくなる」こと。これは即レビュー最悪化 ＋ 返金要求に直結する。

有料ゾーン（`<p>ここから先は ¥○,○○○</p>` 以降）の各 h2 セクションを機械的に充実度検証する。

#### 必須 4 項目（1 つでも未達なら該当セクションを rewrite）

1. [ ] 地の文（`<p>` タグの本文）の合計が **500 字以上**
2. [ ] 具体的な手順・ステップが **3 個以上**（`<p><strong>①</strong>`〜`<p><strong>⑤</strong>` 形式、または連続した独立段落でのステップ表現）
3. [ ] 以下のいずれかが **必ず 1 つ以上** ある:
   - プロンプト全文（`<blockquote>` 内のコピペ可能な Claude 指示文）
   - コード全文（`<pre><code>` 内の動作可能なコード）
   - Before/After 実物（`<blockquote>` 内の改修前・改修後ペア）
   - 実際のクライアントメッセージ抜粋（匿名化済み）
   - 具体的数値データ（作業時間・金額・件数の実測値）
4. [ ] 抽象ワードの使用数が **5 個以下**:
   - 対象ワード: 「効率化」「時短」「重要です」「便利です」「活用する」「様々な」「いろいろ」「柔軟に」「適切に」「効果的に」
   - 5 個超えは具体性不足の指標。具体名・数字に置き換える

#### 推奨 2 項目（1 つ以上あると望ましい）

5. [ ] 失敗例・つまずき例の記述（「自分が詰まったポイント」「こうすると失敗する」）
6. [ ] 応用展開の示唆（「他ジャンルへの転用」「次のステップ」）

#### 判定

- **必須 4 項目すべて OK** → 通過
- **必須 4 項目のうち 1〜2 項目未達** → 該当セクションのみ rewrite（fact-checker / prompt-validator と連携して具体例・プロンプト追加）
- **必須 3 項目以上未達** → **有料ゾーン全体の書き直しを推奨**（記事として商品性を満たさない）

#### 自動検出コマンド例

有料ゾーンの抽象ワード使用数を数える:
```bash
FILE={html ファイルパス}
awk '/ここから先は ?¥/,0' "$FILE" | \
  grep -oE '効率化|時短|重要です|便利です|活用する|様々な|いろいろ|柔軟に|適切に|効果的に' | wc -l
```

有料ゾーンの各 h2 セクションの文字数を数える（Python）:
```python
import re
with open(file_path) as f:
    html = f.read()
paid = html.split('ここから先は')[-1]
sections = re.split(r'<h2[^>]*>([^<]+)</h2>', paid)
for i in range(1, len(sections), 2):
    title = sections[i]
    body = sections[i+1] if i+1 < len(sections) else ''
    text = re.sub(r'<[^>]+>', '', body)
    print(f'{title}: {len(text)} 字')
```

#### 出力フォーマット

```
📊 有料部分充実度ゲート結果

有料ゾーン h2 セクション数: {N}
  ✅ 通過: {n} セクション
  ⚠️ rewrite 必要: {n} セクション
  ❌ 商品性不足（必須 3 項目超未達）: {n} セクション

rewrite 必要セクション詳細:
  1. [h2 タイトル]
     未達項目: 項目 {n}（理由: {具体内容}）
     現状文字数: {n} 字（目標 500 字以上）
     具体手順数: {n} 個（目標 3 個以上）
     具体的証拠の有無: プロンプト全文={有/無} / コード全文={有/無} / Before/After={有/無} / 実メッセージ={有/無} / 数値={有/無}
     抽象ワード使用数: {n} 個（目標 5 個以下）
```

### Step 2.8: 強調密度自動付与ロジック（必須）

Brain 販売ページの購入転換率は**太字密度**に強く相関する。競合分析実測:

| 記事 | 本文 | strong | 密度（/1000字） |
|---|---|---|---|
| メアリ | 21,469 字 | 196 | **9.1** |
| 明鏡 | 14,977 字 | 119 | **7.9** |
| hulu35 | 5,875 字 | 12 | 2.0（薄い） |

**目標密度: 7.0 / 1000字 以上**（明鏡水準）。

#### 自動付与対象パラグラフの判定基準

以下のパターンに該当する段落を優先して**段落全体 `<strong>`** で囲む（ルール H の tiptap バグ回避遵守）:

1. **数字 + 単位**: `\d+\s*(秒|分|時間|日|週|ヶ月|万|円|本|行|%|倍|回|人|章|枚|個|ポイント)` にマッチ
2. **結論フレーズ**: 「〜できる」「〜になる」「〜が変わる」「〜で済む」等の到達状態文
3. **所要時間・到達目標**: `所要時間 \d+分` `到達目標 .*状態` を含む段落（Day 扉系）
4. **Before/After 圧縮表現**: `\d+\s*時間\s*→\s*\d+\s*分` `\d+/\d+` のような劇的変化
5. **インパクト短文**: 20 字以下の独立段落で、命題性のある一文
6. **価格正当化文**: 「1 件受けたら回収できる」「紹介 2 名で元が取れる」等
7. **決めフレーズ**: 記事末・章末の「景色が変わる」「材料は渡した」系

#### 自動付与スクリプト例（ProseMirror / tiptap 環境向け）

```js
const doc = editor.state.doc;
const numberPattern = /\d+\s*(秒|分|時間|日|週|ヶ月|万|円|本|行|%|倍|回|人|章|枚|個|ポイント)/;
const impactKeywords = ['到達目標', '所要時間', '→', 'Before', 'After', '圧縮', '差が', '変わる', '回収', '紹介', '元が取れる'];

const candidates = [];
doc.descendants((node, pos) => {
  if (node.type.name !== 'paragraph') return;
  const text = node.textContent.trim();
  if (!text) return;
  let allBold = true, hasText = false;
  node.descendants(c => {
    if (c.isText) { hasText = true; if (!c.marks.some(m => m.type.name === 'bold')) allBold = false; }
  });
  if (!hasText || allBold) return;
  if (numberPattern.test(text) || impactKeywords.some(k => text.includes(k)) || text.length <= 20) {
    candidates.push({ pos, nodeSize: node.nodeSize });
  }
});

// 目標密度になるまで candidate に bold を付与
const boldMark = editor.schema.marks.bold.create();
let tr = editor.state.tr;
candidates.forEach(({ pos, nodeSize }) => {
  tr = tr.addMark(pos + 1, pos + nodeSize - 1, boldMark);
});
editor.view.dispatch(tr);
```

#### 目標判定

```js
const textLen = editor.state.doc.textContent.length;
const strongCount = document.querySelector('.ProseMirror').querySelectorAll('strong, b').length;
const density = strongCount / textLen * 1000;
// density >= 7.0 を合格。未達なら候補追加して再付与
```

未達時は「数字段落を 1 つ多く拾う」「インパクト短文の閾値を 20→30 字に広げる」で再候補抽出。

### Step 3: 規約チェックリスト

Brain 規約（/terms は 404 のためガイドラインベースで確認）に基づく以下を必ずチェック:

- [ ] 誇大表現がない
- [ ] 個人体験である旨が冒頭・末尾に明記されている
- [ ] 他サービス（note・X等）への誘導リンクが「販売誘導」になっていない（著者プロフィール欄の参照のみ OK）
- [ ] 根拠のない統計・データがない
- [ ] 医療・投資助言系の記述がない
- [ ] 「効果には個人差があります」の免責文がある

### Step 4: 120 点ルーブリック採点

**コンテンツ価値（40 点満点）:**
- 「この記事を買う理由」が明確か（0-10）
- コード・プロンプト・テンプレの実用性（0-10）
- 非エンジニアでも実行可能か（0-10）
- note 版との差分が明確か（30% 以上の追加情報）（0-10）

**Brain 最適化（30 点満点）:**
- タイトルの相場適合性（0-10）
- 商品説明文のフック力（0-10）
- 価格の妥当性（0-10）

**構成適合性（20 点満点）:**
- 冒頭 3 点セット（ペイン→実績→ベネフィット）の有無・順序（0-5）
- H2 見出しの具体性・章タイトルの型（Why 型 / Day 型 / 問いかけ型等・章番号なし）（0-5）
- 無料ゾーン末尾フックの有無・型（6 型のどれか）（0-5）
- Step 2 の 16 項目チェックリスト通過率（14 項目以上 = 5 点 / 11〜13 項目 = 3 点 / 10 項目以下 = 0 点）

**リスク（20 点満点）:**
- 誇大表現スウィープ実施済み（0-10、未実施は 0 点固定）
- 匿名化・免責文（0-10）

**voice 一貫性（10 点満点）:**
- 「文系大学生の自分」の一人称保持（0-5）
- AI 定型フレーズがない（0-5）

**判定:**
- 100 点以上 → 公開可
- 80〜99 点 → 軽微修正で公開可
- 80 点未満 → 自動 rewrite（最大 2 回）、2 回でも 80 未満ならユーザー報告

**採点結果の表示:**
```
📊 品質スコア: {点}/120 {判定}
  コンテンツ価値: {点}/40
  Brain最適化:    {点}/30
  構成適合性:     {点}/20
  リスク:         {点}/20
  voice一貫性:    {点}/10
```

---

## Phase 6: 保存・出力

### Step 1: 前回の `brain/today/` を正規フォルダに移動

`brain/today/` フォルダの中身を確認し、ファイルがあれば正規フォルダへ移動:
- `brain/today/*.md` → `brain/drafts/{記事タイプ}/`
- `brain/today/*.html` → `brain/drafts/{記事タイプ}/`

（`brain/today/` が空ならスキップ）

### Step 2: 今回のファイルを今日用と正規フォルダの両方に保存（HTML + Markdown）

**本文ファイル（HTML版 — Brain エディタへの投稿はこれを貼る）:**
1. `brain/drafts/{記事タイプ}/brain_{日付}_{トピック要約}.html` — バックアップ
2. `brain/today/brain_{日付}_{トピック要約}.html` — 投稿用（Brain エディタにペースト・装飾が自動反映）

**本文ファイル（Markdown版 — バックアップ・編集・後続ツール連携用）:**
3. `brain/drafts/{記事タイプ}/brain_{日付}_{トピック要約}.md` — バックアップ
4. `brain/today/brain_{日付}_{トピック要約}.md` — すぐ開ける場所

**メタファイル（タイトル・説明文・価格・タグ・還元率を 1 つにまとめる）:**
5. `brain/drafts/{記事タイプ}/brain_{日付}_{トピック要約}_meta.md` — バックアップ
6. `brain/today/brain_{日付}_{トピック要約}_meta.md` — 投稿用

**HTML版と Markdown版は同じ内容で書式だけ違う**（HTML 版は note と同じタグで組む）。片方だけ編集して乖離するのを避けるため、1回で両方生成する。

### Step 3: 本文ファイルのフォーマット

冒頭メタ情報（HTML コメント、Brain ペースト時に削除）:
```markdown
<!--
source_note: {note 版のファイルパス or URL}
type: jituroku / knowhow
score: {Phase 5 の総合スコア}
created: YYYY-MM-DD
status: final
-->

# {記事タイトル}

{本文}
```

### Step 4: メタファイルのフォーマット

**重要（2026-04-22 実機確認）**: Brain エディタの投稿画面に「サブタイトル」「商品説明文（購入前表示）」の入力欄は存在しない。タイトル欄のみ。

- 「サブタイトル」は参考メモとして meta.md に残す（将来 Brain に欄が追加された場合・note 併用投稿時の副題候補・本文冒頭 blockquote の文言検討用）
- 「商品説明文（購入前表示）」は meta.md に含めない（本文冒頭 blockquote + 無料ゾーンがその役割を果たす）

```markdown
# Brain 投稿メタ情報

## タイトル
{採用タイトル}

※ ルール L 遵守。「完全」「○○付き」「教科書」「バイブル」系 NG。「実戦メモ」「実録」「作業ログ」等の控えめ表現を推奨。

## サブタイトル（参考メモ・Brain 側に入力欄なし）
{サブタイトル}

## カテゴリ
{Brain カテゴリ選択候補: AI / 副業 / 勉強法 等}

## タグ
{タグ1}, {タグ2}, {タグ3}, ...

## 価格
¥{価格}

## 紹介還元率
{%}%

## 免責文
本記事は個人の体験談であり、同様の成果を保証するものではありません。

## 投稿チェックリスト
- [ ] タイトル入力
- [ ] 本文ペースト（brain_{日付}_{トピック}.html を Phase 6.5 JavaScript API 経由・Ctrl+V 禁止）
- [ ] サムネ画像アップロード（Phase 6 のプロンプト 1〜4 から 1 つ選択）
- [ ] 本文差し込み画像（記事タイプ別枚数目安・{記事ID}_images.md のプロンプト 6 タイプから選択・保存先 `brain/today/images/{記事ID}/{N}.png`）
- [ ] カテゴリ選択
- [ ] タグ設定
- [ ] 価格設定: ¥{価格}
- [ ] 紹介還元率設定: {%}%
- [ ] プレビュー確認（Phase 6.5 の 3 点検証 + 審査通過自動スキャン全 16 項目 0 件確認）
- [ ] 公開
```

### Step 4.5: ブラウザ操作用タブの事前準備（必須）

Step 5 の完了サマリーを出力する**前**に、Brain エディタ貼り付け用の MCP 管理下タブを 1 枚用意しておく。ユーザーが完了サマリーを読んだ瞬間にログインしてエディタ画面に遷移し、そのまま Phase 6.5 の本文流し込みに直行できる状態を作るため。

手順:

1. `mcp__claude-in-chrome__tabs_context_mcp` を `createIfEmpty: true` で呼ぶ（MCP グループ未作成なら新規作成・空タブが 1 枚立つ）
2. 既にグループがあり空タブがない場合は `mcp__claude-in-chrome__tabs_create_mcp` で 1 枚追加
3. 完了サマリーの TODO 【1】に「立ち上げ済みの MCP 管理タブで brain-market.com にログイン → 新規投稿エディタ画面まで遷移」を含める

**ブラウザ拡張が未接続で失敗した場合:**
エラーメッセージをそのままユーザーに伝え、完了サマリーの末尾に「Chrome 拡張の再接続を https://claude.ai/chrome で確認し、接続後に『タブ開いて』と依頼してください」と 1 行添えて継続する。スキル自体はブロックしない。

### Step 4.8: 審査通過自動スキャン（必須・Phase 6 完了前の最終チェック）

2026-04-22 Brain 実機で即レス否認を受けた事例（「短期間でお金持ちに」判定）を受けて追加。テキスト自動判定に引っかからないよう、完了サマリー出力前に本文・タイトル・メタ全体を正規表現で自動スキャンする。

#### 自動スキャン項目

| # | 検知パターン | 対応する絶対ルール | アクション |
|---|---|---|---|
| 1 | 本文・タイトルに `月\s*\d+\s*万` | K | 金額削除 or 期間を外して概算化 |
| 2 | 本文・タイトルに `\d+\s*ヶ月.*月\s*\d+` | K | 期間+金額の組み合わせ解消 |
| 3 | 本文に `¥\s*\d{1,3}[,，]\d{3}` | K | 案件単価を抽象化 |
| 4 | 本文に `辿り着いた\|手に入れ\|ゼロから` | K | 情報商材定番フレーズ削除 |
| 5 | 本文に `たった\s*\d\|全部(直る\|できる\|変わる)\|必ず\s*(稼\|成功\|達成)` | K | 過剰訴求を緩和 |
| 6 | 本文に `文系大学生\|ただの\s*(文系\|素人)` | K | 抽象的自己紹介に置換 |
| 7 | タイトルに `完全(教科書\|攻略\|ガイド\|公開\|保証)` | L | タイトル推奨型に置換 |
| 8 | タイトルに `(\d+\|○)大?特典\|バイブル\|聖典` | L | タイトル推奨型に置換 |
| 9 | タイトルに `稼げる\|儲かる\|必ず\s*(稼\|成功)\|保証` | L | タイトル推奨型に置換 |
| 10 | 冒頭 blockquote（2段落・段落全体 `<b>`）が存在しない | M | テンプレから冒頭 blockquote 挿入 |
| 11 | strong 密度 < 7.0 / 1000字 | H + Step 2.8 | Step 2.8 の自動付与ロジック再実行 |
| 12 | 空段落（`<p></p>` 相当・textContent 空）が 1 つ以上存在 | Phase 6.5 | 一括削除（テンプレあり） |
| 13 | `<ul>/<ol>/<table>/<li>` の存在 | Phase 2 絶対ルール | 段落ペア or pre/code に変換 |
| 14 | 段落中間 `<b>/<strong>`（`<p>[^<]+<b>`） | H | 段落分割して段落全体に `<b>` |
| 15 | blockquote 連続 3 個以上（画像プレースホルダー除く） | J | 段落ペアに変換 |
| 16 | 画像プレースホルダーが記事タイプ目安の下限未満 | Phase 6 画像配置 | タイプ別下限（教科書 11・実録 7・ツールキット 8）まで追加 |

#### スキャン実行スクリプト（例）

```bash
FILE="brain/today/brain_{日付}_{トピック}.html"

echo "=== 審査通過自動スキャン ==="
echo "# 1. 月N万 直接表記:"
grep -nE '月\s*[0-9０-９]+\s*万' "$FILE" | head -10
echo "# 2. 期間+金額の組み合わせ:"
grep -nE '[0-9０-９]+\s*ヶ月.*月\s*[0-9０-９]+' "$FILE"
echo "# 3. 案件単価:"
grep -nE '¥\s*[0-9]{1,3}[,，][0-9]{3}' "$FILE"
echo "# 4. 情報商材定番フレーズ:"
grep -nE '辿り着いた|手に入れ|ゼロから' "$FILE"
echo "# 5. 過剰訴求:"
grep -nE 'たった\s*[0-9]|全部(直る|できる|変わる)|必ず\s*(稼|成功|達成)' "$FILE"
echo "# 6. 煽りキャラ設定:"
grep -nE '文系大学生|ただの\s*(文系|素人)' "$FILE"
echo "# 13. 禁止タグ:"
grep -nE '<(ul|ol|table|li)' "$FILE"
echo "# 14. 段落中間 b:"
grep -nE '<p>[^<]{5,}<b>' "$FILE"

# strong 密度・空段落・blockquote 連続は Brain エディタ側（ProseMirror）で検証
```

#### Phase 6 完了可否

- 全 16 項目の検出件数が 0 件 → Phase 6 完了サマリー出力へ
- 1 件でも検出 → 自動修正できる項目（11, 12, 14, 15, 16）は修正実行、それ以外（1-10, 13）は該当箇所をユーザーに提示して修正方針（ルール K/L/M 参照）を添えて再確認を求める
- 最大 3 回修正ループ後も残件あり → 残件と推奨修正案をまとめて完了サマリーに警告セクションを追加（ユーザー側の判断材料として）

### Step 5: 完了サマリー出力

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Brain 用ドラフト完成

タイトル : {採用タイトル}
タイプ   : {実録 / ノウハウ}
価格     : ¥{価格}
還元率   : {%}%
スコア   : {Phase 5 総合}/100

ファイル:
  本文 → brain/today/brain_{日付}_{トピック}.md
  メタ → brain/today/brain_{日付}_{トピック}_meta.md

想定収益（月 5 部販売）: ¥{金額}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 TODO

【1】Brain に投稿
  1. https://brain-market.com/ にログイン
  2. 「コンテンツを投稿する」をクリック
  3. メタファイル（_meta.md）の投稿チェックリストに従って入力
  4. 本文ファイルの内容をペースト
  5. サムネをアップロード（↓ のプロンプトで Gemini 生成）
  6. 公開

【2】公開後の記録
  Notion「note記事管理」DB の対象レコードに以下を更新:
  - Brainステータス → 投稿済み
  - Brain URL → 公開後の URL
  - Brain 公開日 → 今日の日付

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🖼️ サムネプロンプト（gemini.google.com の画像生成に貼り付け）

{下記テンプレを使用}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### サムネイル生成プロンプト（Brain 特化版・2026-04-20 実地競合分析ベース）

**参照元**: `D:/Claude/bussines/search/brain-thumbnail-analysis-2026-04-20.md`（Brain 売れ筋 20 件視覚要素分析）

#### Brain サムネの 3 大パターン（把握必須）

- **パターン A 情報商材ゴールド型（40%）**: 漆黒 + 金ピカグラデ + 「完全攻略」「教科書」+ 特典バッジ（リツト・ベク等）
- **パターン B ゲーム/アニメ ド派手ネオン型（30%）**: 黒 + 虹色ネオン光線 + アニメキャラ + 月○万円激デカ（メラメラくん・トシノ等）
- **パターン C テキスト密度型（25%）**: 濃紺/黒 + 白テキスト多段 + ツール名大 + チェックマーク（n8n 攻略バイブル等）
- **パターン D（例外）ライト系クリーン型（5%）**: オフホワイト + ツールロゴ + 顔写真丸型（しゅうへいの Claude 教科書のみ）

#### 優太が狙うポジション

**「パターン A 情報商材ゴールド型 + パターン B ゲーム/アニメ ド派手ネオン型の融合」**

- 漆黒背景は維持（Brain 相場の 90% がダーク。白系にするとカテゴリ内で浮く）
- 炎エフェクト・3D金ピカベベルテキスト・スターバースト・特典バッジ複数配置を積極採用
- 「文系大学生・実直」ブランド制約は外し、Brain カテゴリに溶け込む派手路線に寄せる
- 目指すのはパチンコ広告・情報商材広告の最大限の派手さ。「稼げる・すごい」の圧倒的な視覚インパクト
- 成功例参照: `D:\Claude\bussines\Gemini_Generated_Image_oznf27oznf27oznf.png`（炎＋金ピカ3D数字＋バッジ6〜8個＋黄金コイン＋雷）

#### note との違い（展開時の混同防止）

| 比較軸 | note サムネ | Brain サムネ（優太版・派手路線） |
|---|---|---|
| 背景色 | オフホワイト/ライトグレー | 漆黒 + 炎エフェクト + スターバースト |
| テキスト色 | ダークグレー | 金ピカ3Dベベル + 白 + 赤・黄バッジ多色 |
| フォントウェイト | Regular〜Medium | Ultra Bold 全統一 |
| 文字密度 | タイトル 1〜2 行のみ | 3〜5 行 + サブコピー + バッジテキスト多数 |
| 数字の扱い | なし or 小さく | 3D金属ベベルで画面占有率 40%の巨大配置 |
| 装飾要素 | 幾何学シェイプ | 炎・雷・黄金コイン・キラキラ・スターバースト・SVGアイコン縦並び |
| バッジ | なし | 「今だけ」「必見！」「保存版」「スゴ！」「○大特典付き」を 6〜8 個散乱 |
| 雰囲気 | 知的・クリーン・記事メディア | パチンコ広告・情報商材ゴールド路線の最大派手さ |

---

#### 記事タイプ別プロンプト 4 種

以下の 4 タイプから記事タイプに応じて選択してユーザーに出力する。Gemini / Nano Banana（gemini.google.com）に貼り付けて画像生成する想定。

---

##### プロンプト 1: 実録記事型（`jituroku` + 受注・納品・副業ワークフロー系）

派手路線刷新版 — 炎・金ピカ・スターバースト満載、成功例Image#1系の再現性重視。

```
16:9横長の横幅1386px前後のBrain商品サムネイル画像。情報商材ゴールド路線の派手で迫力あるデザイン。背景は漆黒（#000000）で、画面全体に轟く炎エフェクトが広がっている。炎は画面下部から左にかけてオレンジ・赤・黄色が混合した本物の炎のように燃え盛り、その上に金色と赤の放射状スターバーストが爆発風に散っている。左上から中央にかけて白い稲妻の雷エフェクトが走っている。画面右端には黄金コインが複数散乱している。画面各所に白いキラキラスパークルの光粒が散在している。

画像の左側60%のエリアに、以下の日本語テキストを配置する。最上部に赤い丸型バッジで「実録」という白文字を入れる。その下に「{短縮タイトル（例: Claude Design 日本語LP）}」を超極太の金ピカ3Dベベルゴシック体で非常に大きく表示する。テキストはゴールドから黄色へのグラデーションで、縁取りは黒く、立体的な金属光沢を持つ。さらに下に「{サブキャッチ（例: 破綻ゼロ完全攻略）}」を白縁付き超極太ゴシックで大きく配置する。その下に「{実績フレーズ（例: 文系大学生が1週間詰まって辿り着いた答え）}」「{補足訴求（例: コード全文・プロンプト全文同梱）}」を白い小文字で2行添える。

画像の右側40%のエリアには、「{最大実績数字（例: 6行）}」という数字を超極太3D金属ベベルフォントで画面右半分を占めるほど巨大に表示する。色はゴールドから黄色への強烈なグラデーションで金属的な光沢と影を持ち、左右から光が当たって輝いている。その下に「{単位フレーズ（例: のCSSで解決）}」を白文字で添える。数字の周囲には赤とゴールドのスターバーストが爆発している。

画像の中央エリアには白いフラットアイコン（コード・ファイル・設定など技術系を示す4〜5個のシルエット）を縦に並べて配置する。

画面の各所に以下のバッジを散乱配置する。赤地に白文字の吹き出し型「今だけ」バッジを上部と下部に各1個。黄地に黒文字の長方形「必見！」バッジ。黄地に黒文字の「保存版」バッジ。ゴールド地に白文字の「スゴ！」バッジ。黒地に黄文字の「コード全公開」バッジ。これらバッジを画面の四隅と中間に計6〜8個散らす。

全体の配色は黒・赤・ゴールド・黄・白・オレンジの6色。フォントは超極太ゴシック全統一。全体の雰囲気はパチンコ広告・情報商材風の最大限の派手さで、「稼げる・すごい」という圧倒的な視覚インパクトを持つ。

日本語テキストは全て正確に描画してください。
```

---

##### プロンプト 2: ノウハウ / 教科書型（`knowhow` + 「教科書」「完全ガイド」系）

金ピカ「教科書」浮彫りテキスト・黄金バッジ・特典○大プレゼント系。

```
16:9横長の横幅1386px前後のBrain商品サムネイル画像。情報商材ゴールド型の重厚な教科書デザイン。背景は漆黒（#000000）から左にかけて深い宇宙の星雲のような青紫のグラデーションが広がり、その上に金色の光線が放射状に広がっている。背景の右下から金色のエネルギー波のような光が放射している。画面各所に白いキラキラスパークルが散在している。

画像の中央〜左寄りのエリアに、以下の日本語テキストを配置する。最上部には「{ツール名（例: Claude Code）}」をゴールドの輝く盾型バッジで囲んで大きく表示する。その下に「{×対象（例: ×クラウドワークス）}」を超極太の金ピカグラデーション3Dベベルゴシック体で2行にわたり非常に大きく表示する。文字色はゴールドから白への強烈なグラデーションで、黒い縁取りがあり立体的な浮き彫り感を持つ。その下に「完全教科書」または「完全攻略」のテキストを赤地の黄文字でさらに大きく表示する。

画像の右側35%のエリアに、光り輝く黄金の教科書のイラストを配置する。教科書の表紙には「教科書」の金文字が光る浮き彫り刻印のように描かれている。教科書の周囲から金色の光が放射している。教科書の上下に{ツールロゴ（例: ClaudeのアスタリスクマークとCWのロゴ）}が金色の光と共に浮いている。

画面の左上と右上に「{○大特典付き！（例: 豪華8大特典付き！）}」と書かれた赤いスターバースト型バッジを大きく配置する。画面右上にゴールドの丸型バッジで「{数量（例: 全16章）}」を表示する。下部には「{訴求点1（例: 案件選定→応募→納品まで完全解説）}」「{訴求点2（例: プログラミング経験ゼロからOK）}」「{訴求点3（例: 実際の受注記録をそのまま公開）}」を黄地・黒文字のタグ型バッジを横並びで3個配置する。

画面各所に「今だけ」「必見！」「限定公開」の赤い吹き出し型バッジを3〜4個散らす。

全体の配色は黒・ゴールド・赤・白・黄の5色。フォントは超極太ゴシック。全体の雰囲気は「これが完全版だ」という圧倒的な重厚感と高価値感。金ピカと赤の組み合わせで「権威ある教科書」を演出する。

日本語テキストは全て正確に描画してください。
```

---

##### プロンプト 3: ツールキット型（テンプレ集・プロンプト集・GPTs 付き商品）

30種・50本のような大量数字訴求 + 宝箱イメージ・ギラギラ。

```
16:9横長の横幅1386px前後のBrain商品サムネイル画像。情報商材ツールキット型の豪華絢爛なデザイン。背景は漆黒（#000000）で、中央から右にかけてゴールドと赤のグラデーション光がフレアのように広がっている。画面下部の中央から炎がメラメラと燃え上がり、上へ向かって広がっている。背景に散らばる白いスパークル光粒が多数。

画像の左側55%のエリアに、以下を日本語で配置する。最上部に黒地・金縁の横長バナーで「今すぐ使える！完全パッケージ」と白文字で書く。その下に「{同梱数（例: 30）}」という巨大な数字を超極太3D金属ベベルフォントで表示する。色はゴールドから黄色への強烈なグラデーションで金属光沢を持ち、数字の周囲から金色の光が放射している。その直下に「{何本か（例: 本の応募文テンプレ）}」を白縁付き超極太ゴシックで大きく表示する。さらに下に赤いセパレーターラインを挟んで「+ {追加要素（例: GPTs同梱）}」を黄文字でやや大きく表示する。

画像の右側45%のエリアに、黄金の宝箱が開いていてその中から金色の光が溢れ出しているイラストを配置する。宝箱の上から金色のコインや星マーク、ドキュメントアイコンが溢れ出している。宝箱の脇に{ツールロゴ（例: Claudeのアスタリスクマーク）}が金色に輝いている。

画面下部に3つの黄地・黒文字のスターバースト型バッジを横並びで配置する。「{特典1（例: コピペで即使える）}」「{特典2（例: 副業に最適）}」「{特典3（例: 初心者OK）}」とそれぞれ書く。

画面の四隅と中間に「今だけ」「限定公開」「スゴ！」「必見！」の赤と黄のバッジを計5〜6個散らす。右上に「{数量バッジ（例: 全30本）}」のゴールド円形バッジを大きく配置する。

全体の配色は黒・ゴールド・赤・黄・白の5色。全体の雰囲気は「宝が詰まっている・今すぐ使える・大量コンテンツ」という圧倒的な豪華感。パチンコの大当たり演出のような金ピカ爆発感が理想。

日本語テキストは全て正確に描画してください。
```

---

##### プロンプト 4: フラッグシップ型（完全ロードマップ・完全公開・高価格帯 ¥7,980〜）

最強に派手・月○万円メガ数字・階段状マイルストーン。

```
16:9横長の横幅1386px前後のBrain商品サムネイル画像。情報商材ゴールド型の最強フラッグシップデザイン。背景は漆黒（#000000）で、画面全体に轟く炎エフェクトが激しく燃え盛っている。炎は画面下部から全面にわたりオレンジ・赤・黄色の混合で燃え上がり、その上に金色と赤のスターバーストが爆発風に広がっている。左から中央に白い稲妻の雷エフェクトが走る。右端に黄金コインが散乱している。画面各所に白いキラキラスパークルが散在している。

画像の左側55%のエリアに、以下の日本語テキストを配置する。最上部に赤地・黄文字のスターバースト型吹き出しバッジで「超実録！」と表示する。その下に「{主軸ツール名（例: Claude Code）}×」を超極太の金ピカ3Dベベルゴシック体で1行目として非常に大きく配置する。文字色はゴールドから黄色への強烈なグラデーション・黒縁取り・金属光沢。2行目に「{対象プラットフォーム（例: クラウドワークス）}」を同様の超極太3D金ピカで配置する。その下に「{商品タイプ（例: 完全ロードマップ公開）}」を赤地・黄文字のタグ型で大きく表示する。最後に「{訴求文（例: 非エンジニアが2ヶ月で稼いだ全手順）}」を白の小文字で添える。

画像の右側40%のエリアに、「{最大実績数字（例: 月14万）}」という金額を超極太3D金ピカベベルフォントで画面右半分を占めるほど巨大に表示する。色はゴールドから白への強烈なグラデーションで金属光沢と影を持ち、数字の周囲から強烈な金色の光が放射している。その下に「{単位（例: 円達成！）}」を白文字でやや大きく表示し、さらに小さく「{補足（例: 2ヶ月目に突破）}」を黄文字で添える。数字の上下にゴールドの炎エフェクトが燃え上がっている。

画面下部に全幅の黒地帯を設け、その中に黄金のSTEPアイコンを横並びで配置する。「STEP1 {ステップ1（例: 案件選定）}」→「STEP2 {ステップ2（例: 応募文作成）}」→「STEP3 {ステップ3（例: 受注）}」→「STEP4 {ステップ4（例: 納品）}」と階段状に並べ、各STEPの矢印はゴールドで繋ぐ。

画面の各所に「今だけ」「限定公開」「必見！」「スゴ！」「保存版」の赤・黄バッジを計6〜8個派手に散らす。右上には「{特典数（例: 10大特典付き）}」の赤いスターバースト型バッジを大きく配置する。

全体の配色は黒・炎（赤・オレンジ）・ゴールド・黄・白の5色。フォントは超極太ゴシック全統一。全体の雰囲気はパチンコ大当たり演出・情報商材広告の最大限の派手さで、「これを買えば月に何十万も稼げる」という圧倒的なインパクト・高揚感・緊急感を与える。

日本語テキストは全て正確に描画してください。
```

---

#### プロンプト選択ロジック（スキル内部で自動判定）

記事タイプと商品特性から以下のルールで選択:

1. 記事タイプが `jituroku` → **プロンプト 1（実録記事型）**
2. 記事タイプが `knowhow` かつタイトルに「教科書」「完全ガイド」「バイブル」を含む → **プロンプト 2（教科書型）**
3. 商品にテンプレ・GPTs・プロンプト集・設定ファイル等が同梱 → **プロンプト 3（ツールキット型）**
4. 価格 ¥7,980 以上 または タイトルに「完全ロードマップ」「完全公開」「全手順」を含む → **プロンプト 4（フラッグシップ型）**

複数条件に該当する場合は優先度 4 > 2 > 3 > 1 の順に選ぶ。

#### プレースホルダ置換ルール

上記プロンプト内の `{...}` は以下で置換:

- `{短縮タイトル}`: 記事タイトルから 15〜20 字以内に短縮
- `{実績フレーズ}`: 「文系大学生が 2 ヶ月でやったこと」「非エンジニアが Claude Code だけで」等
- `{ツール名}` `{×対象}`: 「Claude Code」「×クラウドワークス」等
- `{最大実績数字}`: 優太の場合は「14万」固定（または記事固有の数字）
- `{訴求点 1〜3}`: 商品の核心メリット 3 点を選定
- `{ステップ 1〜4}`: フラッグシップ商品の主要ステップ

#### NG リスト（全プロンプト共通の禁止要素）

派手路線では現行の抑制系 NG のほとんどを撤廃・解禁する。以下のみ維持:

**技術的 NG（視認性・品質のため維持）:**
1. テキスト 8 行以上の詰め込み（一覧表示時 150px 前後に縮小された際に判読不能）
2. 5 色以上の文字色混在（ただし「黒・白・赤・ゴールド・黄」の 5 色セットは許可範囲内）
3. 白背景に白縁テキスト（コントラスト不足で可読性ゼロ）
4. 横長の英語テキストのみ（Brain 購入者層は日本語優位）

**コンテンツ的 NG（誇大広告・法的リスク）:**
5. 「月○万円保証」「確実に稼げる」等の確定的成果表現（景表法リスク）
6. 架空の特典数・架空の販売実績バッジ（実際にない「○部突破」等）
7. 競合商品名の誹謗中傷・虚偽比較

**ブランド上の最低限 NG:**
8. 本人の実際の顔写真のみ（テキスト・エフェクトなし）— 実績未確立段階では逆効果
9. 成人向け・同人誌感

**撤廃した現行 NG（派手路線では積極推奨に転換）:**
- 金ピカグラデーションテキスト → **推奨**（3D ベベルは派手路線の核心）
- AI 生成の宇宙/星雲背景 → **使用可**
- 「豪華○大特典付き」バッジ → **推奨**
- 月桂樹バッジ → **使用可**
- ネオン放射光線背景 → **推奨**
- 炎・キラキラ・爆発エフェクト → **強く推奨**
- パチンコ・スロット的な派手さ → **目指すべきゴール**

---

### 記事内差し込み画像プロンプト生成（必須・サムネとは別）

販売ページ（Brain 公開部分）での購入転換率はビジュアル訴求に依存する。競合分析（2026-04-22 実施・メアリ 148部¥24,980 / 明鏡 ¥29,800 / hulu35 ¥3,980）で判明した事実:

| 記事 | 価格 | 画像枚数 |
|---|---|---|
| メアリ | ¥24,980 | 78 |
| 明鏡 | ¥29,800 | 23 |
| hulu35 | ¥3,980 | 29 |

**結論**: Brain で「画像ゼロ」はリジェクト級。最低 15 枚・推奨 20 枚以上を狙う。本文差し込み画像は「サムネとは別」に生成する必要がある。

#### 枚数目安（記事タイプ別）

| 記事タイプ | 差し込み画像 | 内訳例 |
|---|---|---|
| 教科書型（Day 別カリキュラム） | 11〜16 枚 | 冒頭約束 1 + 実績証拠 2 + Day 別 7 + Before/After 3 + 章末 1〜3 |
| 実録型 | 7〜10 枚 | 冒頭訴求 1 + 実績証拠 2 + 本編ストーリー 3〜5 + 総まとめ 1〜2 |
| ツールキット型 | 8〜12 枚 | 冒頭訴求 1 + 同梱物カタログ 5〜7 + 使用例 2〜3 + 特典 1 |

#### プレースホルダー記法（記事 HTML 内）

**絶対に見落とさない形式**で本文 HTML に埋める。ユーザーが Brain エディタ上で画像をアップロードする際の目印として機能させる。

```html
<blockquote>
<p><strong>🔴🔴🔴 画像N「{ラベル}」挿入位置 🔴🔴🔴</strong></p>
<p><strong>⚠️ 生成プロンプトは {記事ID}_images.md の「## 画像N」を参照</strong></p>
</blockquote>
```

- blockquote + 太字 + 🔴絵文字 3 連続で視覚的に最大限目立たせる（Brain エディタ上でも紫系背景で表示される）
- `N` は通し番号（1 から）
- `{ラベル}` は何の画像かわかる短文（例: 「7つの約束」「月14万実績」「Day1扉」）
- `{記事ID}` は `brain_{YYYYMMDD}_{suffix}` 形式
- 本番時はユーザーが画像をアップロード → このブロックを削除 or 画像ノードで置換

#### 画像プロンプトファイル `_images.md` 生成

本文 HTML（`brain_{date}_{suffix}.html`）・メタ（`_meta.md`）と同階層に、画像プロンプト専用ファイルを生成:

- ファイル名: `brain_{YYYYMMDD}_{suffix}_images.md`
- 場所: `brain/today/`（本文と同じディレクトリ）

#### 生成画像の保存先（記事ID単位のサブフォルダ・git 管理対象）

Gemini / Nano Banana で生成した画像ファイルは以下に保存:

```
brain/today/images/
└── {記事ID}/
    ├── thumb.png              ← サムネ（記事ヘッダー用・16:9）
    ├── img01_{label}.png      ← 本文差し込み画像 1
    ├── img02_{label}.png
    └── ...
```

**命名規約**:
- サブフォルダ名 = 記事ID (`brain_{YYYYMMDD}_{suffix}`)
- ファイル名 = `{N}.png`（N は 1〜99 の通し番号・画像N と対応・ゼロ詰め不要）
- サムネは `thumb.png` 固定
- 拡張子は `.png` 推奨（Brain の透明度処理が JPEG より安定）

**git 管理**:
- 画像はリポジトリに含める（版管理・再利用・次記事テンプレ流用のため）
- 1 枚 1〜3MB 想定・15 枚 + サムネで最大 50MB 未満なら許容
- 肥大化が懸念される場合は `.gitignore` に `brain/today/images/` を追加してローカル管理に切り替え
- フォルダは空でも `.gitkeep` を置いて構造を保持

ファイル構造テンプレ:

```markdown
# 画像プロンプト集: {記事ID}

> 本文 `{記事ID}.html` 内のプレースホルダー [画像N] に対応する Gemini 画像生成プロンプト集。
> 参照ガイド: `gemini_prompt_guide.md`（日本語自然言語パラグラフ・英語構造化 NG）

## 共通注意事項

- Gemini アプリ（ブラウザ）はアスペクト比指定が 1:1 に固定されるケースあり（2026-03 時点ユーザー報告多数）。縦長・横長希望の場合は API 直叩き or Nano Banana Pro を推奨
- ネガティブプロンプトは Imagen 3.0-generate-002 以降・Gemini 2.5 Flash Image で非対応。「除外したい要素をポジティブな表現で置き換える」こと
- 全プロンプトの末尾に「日本語テキストは全て正確に描画してください。」を必須付与
- 配色はサムネと統一（黒・ゴールド・赤・黄・白の 5 色）

---

## 画像1: {ラベル}

- **配置位置**: {記事内のどこか・例: 冒頭 blockquote の直後}
- **プレースホルダー**: `[画像1: {ラベル}]`
- **アスペクト比**: {3:4 or 16:9 or 1:1}
- **サイズ推奨**: {例: 1080×1440}
- **タイプ**: {約束カード型 / 実績証拠型 / ロードマップ型 / Before/After 型 / 章扉型 / チェックリスト型}

(プロンプト本文)

---

## 画像2: {ラベル}
...
```

#### 本文差し込み画像プロンプト 6 タイプ（サムネ 4 タイプの姉妹）

公式ガイド（`gemini_prompt_guide.md`）準拠の日本語自然言語パラグラフ形式。

##### タイプ A: 約束カード型（冒頭訴求用・縦長 3:4）

冒頭 blockquote 直後に配置。メアリ「6つの誓い」の情報商材型レイアウトを踏襲しつつ配色は優太サムネと統一。

```
3:4縦長の1080x1440ピクセルBrain本文差し込み画像。情報商材ゴールド型の重厚で派手な「約束カード」デザイン。背景は漆黒（#000000）から深い宇宙の星雲のような青紫（#1a0a3e）のグラデーション。上部から金色の光線が放射状に広がり、背景全体に白いキラキラスパークルが散在している。画面全体の外周を金色（#FFD700）のネオン枠線で囲み、枠の四隅に金色の装飾パーツがある。

画面上部中央に、以下の日本語テキストをゴールドから白への強烈なグラデーションの超極太3Dベベルゴシック体で配置する。最上段に「{N}つの約束」を画面横幅の70%を使って超巨大に表示（黒縁取り・金光沢・3D浮き彫り）。その下に白文字の通常ゴシック体で「{サブコピー（例: 本教科書はあなたに{N}つの未来を約束します）}」を2行で配置する。その下に赤と金のスターバースト型バッジで「{特典数（例: 豪華8大特典付き！）}」を斜めに配置する。

画面中央から下部にかけて、{N}項目を番号付きカード型で縦に並べる。各項目は左端に金色グラデーションの円形番号バッジ（直径80px・赤縁取り・3D立体）に白文字で1〜{N}を大きく入れる。番号右側に白文字の説明テキストを配置し、重要な数字・結果部分は黄色（#FFEB3B）ハイライトで強調する。各カードの区切りは金色の破線。

項目1（番号1）: {約束1（数字・結果は黄ハイライト）}
項目2（番号2）: {約束2}
...
項目{N}（番号{N}）: {約束{N}}

画面最下部に赤地・黄文字のタグ型バッジで「{締めフレーズ（例: 文系大学生が月14万円まで辿り着いた教科書）}」を横幅いっぱいに表示する。

全体の配色は黒・ゴールド・白・黄・赤の5色統一。フォントは超極太ゴシック体で全項目統一。全体の雰囲気はパチンコ広告・情報商材ゴールド路線で、「稼げる・すごい・未来が手に入る」という圧倒的な視覚インパクト。

日本語テキストは全て正確に描画してください。
```

##### タイプ B: 実績証拠型（数字訴求・横長 16:9）

「月14万」「3時間→15分」「1/12に圧縮」等の実績数字を主役にした証拠風画像。h2 「〜実績」の直後に配置。

```
16:9横長の1920x1080ピクセルBrain本文差し込み画像。情報商材ゴールド型の実績証拠カード。背景は漆黒（#000000）で、中央から金色とオレンジのグラデーション光が爆発風に放射している。画面下部の両端から炎がメラメラと燃え上がり、画面全体に白いキラキラスパークルが散在している。

画面中央に「{最大実績数字（例: 月14万円）}」という数字を超極太3D金属ベベルフォントで画面幅の60%を使って超巨大に表示する。色はゴールドから白への強烈なグラデーション・黒縁取り・金属光沢・3D浮き彫り。数字の上下に赤とゴールドのスターバーストが爆発している。

数字の上に「{前置きコピー（例: 文系大学生が辿り着いた）}」を白文字の超極太ゴシック体で配置する。

数字の下に「{後置きコピー（例: クラウドワークス副業2ヶ月目の実績）}」を黄文字の超極太ゴシックで配置する。

画面左下に「{補足内訳1（例: Webアプリ UI 案件 ¥65,000）}」、画面右下に「{補足内訳2（例: + ¥45,000 受注）}」を白文字の中サイズで配置する。

画面四隅に赤地・白文字のスターバースト型バッジで「本物」「実績」「証拠」「実録」を各1個計4個散らす。

全体の配色は黒・ゴールド・黄・赤・白の5色。全体の雰囲気は圧倒的な数字インパクトで「本当に稼いでいる」という信頼感を与える。

日本語テキストは全て正確に描画してください。
```

##### タイプ C: ロードマップ型（カリキュラム全体図・横長 16:9）

7 日カリキュラム・フロー全体像を可視化。「カリキュラムの全体像」h2 の直後に配置。

```
16:9横長の1920x1080ピクセルBrain本文差し込み画像。情報商材ゴールド型のカリキュラムロードマップ図。背景は漆黒（#000000）で、上部から金色の光線が斜めに差し込み、画面全体に白いキラキラスパークルが散在している。

画面上部中央に「{タイトル（例: {N}日間カリキュラム全体像）}」を超極太3D金ピカベベルゴシック体で大きく表示する。色はゴールドから白へのグラデーション・黒縁取り・3D立体。

画面中央に{N}個の円形ステップバッジを横並び（または上昇階段状）に配置する。各バッジは金色のグラデーション・赤縁取り・3D立体で、中央に「Day1」「Day2」...「Day{N}」の白文字を大きく入れる。バッジ間は金色の矢印で繋ぎ、左から右へ（または下から上へ）の流れを表現する。

各バッジの下に、白文字で「{Day1タイトル（例: 最初の1枚を30秒で）}」「{Day2タイトル}」...を配置する。各タイトルの下に黄色文字で「{所要時間（例: 20分）}」を小さく添える。

画面左下に「{スタート地点（例: 知識ゼロ・副業ゼロ）}」、画面右下に「{ゴール地点（例: 月14万円の副業ワークフロー完成）}」を赤地・黄文字のタグ型バッジで表示する。

画面各所に「完全攻略」「全公開」「実録」のバッジを計3〜4個散らす。

全体の配色は黒・ゴールド・赤・黄・白の5色。全体の雰囲気は「明確な到達点が見える」という圧倒的なロードマップ感と達成感。

日本語テキストは全て正確に描画してください。
```

##### タイプ D: Before/After 比較型（左右2分割・横長 16:9）

「提案資料 3 時間 → 15 分」等の劇的変化を左右比較で可視化。該当 Day の冒頭に配置。

```
16:9横長の1920x1080ピクセルBrain本文差し込み画像。情報商材ゴールド型のBefore/After比較カード。背景は漆黒（#000000）で、画面中央に金色の縦分割ラインがあり、左右を二分している。

画面上部中央に「{タイトル（例: Claude Design導入で起きた劇的変化）}」を超極太金ピカベベルゴシック体で表示する。

画面左半分（Before）: 背景は青黒いグラデーション。最上部に赤地・白文字で「BEFORE」ラベル。中央に「{Before数字（例: 3時間）}」を超極太3Dグレー・青の金属ベベルフォントで大きく表示する。下部に「{Before説明（例: PowerPointで叩き台作成）}」を白文字で配置する。画面左下に黒いシルエットで疲れた人物アイコン。

画面右半分（After）: 背景は金色とゴールドのグラデーション。最上部に金地・黒文字で「AFTER」ラベル。中央に「{After数字（例: 15分）}」を超極太3D金ピカベベルフォントで大きく表示する。下部に「{After説明（例: Claude Designで自動生成）}」を黄色文字で配置する。画面右下に金色で笑顔のアイコン。数字の周囲から金色の光が放射している。

画面中央の分割ライン上に「×{倍率（例: 1/12）}」または「{改善率（例: 12倍速）}」を赤地・黄文字のスターバースト型バッジで大きく配置する。

画面下部に金色の帯で「{締めフレーズ（例: これが、Claude Designが副業を変える理由。）}」を表示する。

全体の配色は黒・青・金・赤・黄・白の6色。左は「苦しい過去」暗く冷たい配色、右は「輝ける未来」明るく派手な配色で、圧倒的な対比を演出する。

日本語テキストは全て正確に描画してください。
```

##### タイプ E: 章扉型（Day 番号デカ・16:9 or 3:4）

各 Day の冒頭見出し直前に配置。カリキュラムの区切り感を強く演出。

```
16:9横長の1920x1080ピクセルBrain本文差し込み画像。情報商材ゴールド型の章扉カード。背景は漆黒（#000000）から深い紫（#1a0a3e）へのグラデーション。上部から金色の光線が放射状に広がり、画面全体に白いキラキラスパークルが散在している。画面外周を金色のネオン枠線で囲む。

画面中央に「Day{N}」という表記を画面横幅の65%を使って超巨大に表示する。「Day」は超極太金ピカベベルゴシック体・黒縁取り・3D浮き彫り・ゴールドから白へのグラデーション。「{N}」は赤地に黄文字でさらに大きく。数字の周囲から金色の光が放射している。

「Day{N}」の上に「{章テーマ（例: Claude Designの基本操作を掌握する日）}」を白文字の超極太ゴシックで配置する。

「Day{N}」の下に「{到達目標（例: スライド5枚を30秒で生成できる状態）}」を黄色文字の中サイズで配置する。

画面左下に金色のバッジで「所要時間 {X}分」、画面右下に赤地・白文字のバッジで「{難易度（例: 初級）}」を配置する。

画面四隅に金色のスターマークを装飾として散らす。

全体の配色は黒・紫・ゴールド・赤・黄・白の6色。全体の雰囲気は「新しい章が始まる」という高揚感と期待感。

日本語テキストは全て正確に描画してください。
```

##### タイプ F: チェックリスト型（5ポイント・箇条書き画像化・縦長 3:4）

「つまずき 5 ポイント」「6 行 CSS」等の箇条書きを画像化。該当箇所の直後に配置。

```
3:4縦長の1080x1440ピクセルBrain本文差し込み画像。情報商材ゴールド型のチェックリストカード。背景は漆黒（#000000）から深い宇宙の星雲のような青紫（#1a0a3e）のグラデーション。画面全体の外周を金色のネオン枠線で囲む。上部から金色の光線が放射し、画面に白いキラキラスパークルが散在している。

画面上部中央に「{タイトル（例: つまずき5ポイント）}」を超極太金ピカベベルゴシック体で大きく表示する。色はゴールドから白へのグラデーション・黒縁取り・3D立体。その下に白文字で「{サブタイトル（例: 回避できる落とし穴）}」を配置する。

画面中央から下部にかけて{N}個のチェックボックス型カードを縦に並べる。各カードは金色の四角枠・黒背景で、左端に金色の3Dチェックマーク（✓）のアイコンを配置する。アイコンの右側に白文字で「{ポイント1（数字・キーワードは黄ハイライト）}」を配置する。各カードの区切りは金色の破線。

ポイント1: {内容1}
ポイント2: {内容2}
...
ポイント{N}: {内容{N}}

画面最下部に赤地・黄文字のタグ型バッジで「{締めフレーズ（例: これを知れば事故らない）}」を横幅いっぱいに表示する。

全体の配色は黒・ゴールド・白・黄の4色。全体の雰囲気は「具体的で実用的」な情報整理感と、派手なゴールド路線の両立。

日本語テキストは全て正確に描画してください。
```

#### タイプ選択 × 配置ロジック（自動適用）

Phase 5 の本文構造生成時に、以下のルールで画像を配置する:

1. 冒頭 blockquote の直後 → **タイプ A（約束カード）** を必ず 1 枚
2. 「〜実績」系 h2 の直後 → **タイプ B（実績証拠）** を 1〜2 枚
3. 「{N}日間カリキュラム」系 h2 の直後 → **タイプ C（ロードマップ）** を 1 枚
4. 各 Day の h2 直前 → **タイプ E（章扉）** を 1 枚ずつ
5. 「Before/After」「3 時間 → 15 分」系の節 → **タイプ D（比較）** を 1〜3 枚
6. 「{N} ポイント」「つまずき」「{N} 行 CSS」系の節 → **タイプ F（チェックリスト）** を 1 枚
7. 記事末尾の総まとめ → **タイプ A 再掲 or 特典カード**

#### 運用フロー（brain-run 実行時）

1. Phase 5 で本文 HTML を生成する際、上記ロジックで画像プレースホルダーを自動挿入
2. Phase 6 でサムネプロンプト（既存 4 タイプ）+ 本文差し込み画像プロンプト（6 タイプから選択）を生成
3. `_images.md` ファイルに全プロンプトを書き出す（通し番号 1 から）
4. 画像保存用サブフォルダ `brain/today/images/{記事ID}/` を作成（.gitkeep で構造保持）
5. Phase 6.5 の Brain エディタ流し込み時、プレースホルダー入りの HTML を setContent で投入
6. Phase 6 の完了サマリーで以下を明記:
   - `_images.md` の各画像プロンプトを Gemini / Nano Banana Pro にコピペして生成
   - 保存先: `brain/today/images/{記事ID}/img{NN}_{label}.png`（命名規約は上記）
   - Brain エディタの 🔴🔴🔴 プレースホルダー位置に Brain 内蔵の画像アップローダーで挿入
   - アップロード完了後、プレースホルダー blockquote を削除
   - 全画像アップ後、プレビューで目視確認

---

## Phase 6.5: Brain エディタへの流し込み（Ctrl+V 禁止・JavaScript API 経由）

Brain エディタ（https://brain-market.com/a/{ID}/edit）は **tiptap ベース**で、Ctrl+V ペースト経路に重大な「マーク継続バグ」がある。HTMLに `<strong>` が一箇所でもあると、以降の段落すべてが強制的に `<strong>` で包まれて全段落太字化する。

このため、**Brain エディタへの流し込みは必ず以下の手順で行う**。ユーザーに Ctrl+V させるのは禁止。

### 手順

1. Claude が Brain エディタのタブを mcp__claude-in-chrome__tabs_context_mcp で把握（なければ tabs_create_mcp で新規作成してユーザーに Brain エディタページを開いてもらう）
2. `document.querySelector('.tiptap.ProseMirror').editor` で editor instance を取得
3. `editor.commands.setContent(html)` で全置換、または `editor.commands.focus('end')` → `insertContent(html)` で末尾追加

### 分割挿入前の HTML minify（必須）

**流し込み前に必ず HTML を minify する**。段落間の改行・空白を除去しないと、tiptap が改行を hardBreak 付き空段落（`<p><br></p>` 相当、`node.textContent===''` かつ `nodeSize:4`）に変換し、表示上「空のセクションが大量」状態になる。

```js
const cleanHtml = rawHtml
  .replace(/<!--[\s\S]*?-->/g, '')  // HTML コメント除去（先頭メタデータ対策）
  .replace(/>\s+</g, '><')           // タグ間空白・改行除去
  .trim();
editor.commands.setContent(cleanHtml);
```

### 分割挿入（1記事30KB超の場合）

javascript_tool の text パラメータは 1 回あたり 20〜30KB が安全圏。超える記事は分割:

- Part 1: 無料ゾーン前半（~20KB、minify 済） → `editor.commands.setContent(part1)`
- Part 2: 無料ゾーン後半（minify 済） → `editor.commands.focus('end')` → `insertContent(part2)`
- Part 3: 有料ゾーン（minify 済） → `editor.commands.focus('end')` → `insertContent(part3)`

### 検証（必須）

流し込み後、以下 3 点を全てパスすることを確認:

**① 部分太字（マーク継続バグ症状）が 0 件**:

```js
const el = document.querySelector('.tiptap.ProseMirror');
const paragraphs = [...el.querySelectorAll('p')];
const partial = paragraphs.filter(p => {
  const s = p.querySelector('strong');
  return s && s.textContent.trim() !== p.textContent.trim();
}).length;
// partial === 0 であること
```

**② 空段落（見えない空セクション）が 0 件**:

```js
const editor = document.querySelector('.ProseMirror').editor;
let emptyCount = 0;
editor.state.doc.descendants((node) => {
  if (node.type.name === 'paragraph' && node.textContent.trim() === '') {
    emptyCount++;
  }
});
// emptyCount === 0 であること
// ⚠️ content.size === 0 のみでは検知できない。hardBreak だけの段落は size>0 だが表示上は空
```

**③ 空段落が検出された場合は後ろから一括削除**:

```js
const editor = document.querySelector('.ProseMirror').editor;
const positions = [];
editor.state.doc.descendants((node, pos) => {
  if (node.type.name === 'paragraph' && node.textContent.trim() === '') {
    positions.push({ pos, nodeSize: node.nodeSize });
  }
});
let tr = editor.state.tr;
positions.reverse().forEach(({ pos, nodeSize }) => {
  tr = tr.delete(pos, pos + nodeSize);
});
editor.view.dispatch(tr);
```

### 絶対禁止

- ❌ ユーザーに HTML 全文をコピーさせて Ctrl+V させる（全段落太字化する）
- ❌ HTML の段落中間に `<b>` / `<strong>` を書く（Phase 2 H ルール違反）
- ❌ setContent / insertContent の結果を検証せずに完了扱いにする
- ❌ minify を省略して raw HTML を流し込む（空段落が大量発生する）
- ❌ 空段落チェックを `content.size === 0` だけで行う（`textContent.trim() === ''` で判定する）

### 画像・有料ゾーン境界・サムネ設定

HTML 流し込みで本文は完了するが、以下は Brain エディタの UI 操作が別途必要:

- **有料ゾーン境界**: 本文の「ここから先は ¥○,○○○」の後ろで、エディタ UI の「有料部分の開始」ボタンをクリック
- **画像アップロード**: `editor.commands.setImage({src, alt})` で外部 URL 挿入も可能だが、Brain の画像は JPEG/PNG ファイルアップロード運用が基本
- **サムネ**: メインサムネ画像は記事ヘッダーの「メイン画像をアップロードする」から別途アップ

---

## Phase 7: Notion 記録

### 対象記事のレコードを更新

Notion「note記事管理」DB（data_source: `collection://812aa728-8d3e-42e4-a9cd-6a91c303b2c2`）の対象記事レコードに以下フィールドを更新する:

- `Brainステータス`: `草稿完成`
- `Brain 草稿パス`: `{brain/today/ の本文ファイル絶対パス}`
- `Brain 価格`: `¥{価格}`
- `Brain 還元率`: `{%}%`

**対象フィールドが存在しない場合:**
Notion DB に以下のフィールドを追加するようユーザーに案内する:

```
⚠️ Notion「note記事管理」DB に以下のフィールドが未作成です。追加してください:

1. Brainステータス (select) — 選択肢: 未作成 / 草稿完成 / 投稿済み
2. Brain 草稿パス (text)
3. Brain URL (url)
4. Brain 価格 (number)
5. Brain 還元率 (number)
6. Brain 公開日 (date)
7. Brain 売上 (number) — 月次で更新
```

---

## Phase 8: PDCA フィードバック（投稿後）

投稿後にユーザーに促す記録項目:

**投稿直後:**
- Brain URL
- 投稿日
- 最終的に設定した価格・還元率

**1 週間後:**
- 販売数
- Brain 内 PV
- レビュー・コメント

**1 ヶ月後:**
- 累計販売数
- 売上
- アフィリエイター経由の割合
- 学び（何が効いた・効かなかった）

これらは Notion の対象レコードに追記する。次回 /brain-run 実行時に Phase 1 の相場調査と併せて参照される（「前回 X 件売れたから今回は価格を上げよう」等の判断に使う）。

---

## エラーハンドリング

**対象 note 記事が見つからない:**
```
❌ Brain 化できる note 記事が見つかりません。

対処:
1. /note-run を実行して note 記事を作成する
2. または /brain-run {記事パス} で明示的に指定する
```

**記事タイプが速報の場合:**
```
⚠️ この記事は速報タイプです。
速報は鮮度依存のため Brain の長期販売と相性が悪い傾向があります。
それでも Brain 化しますか？ (y/n)
```

**Phase 5 の品質スコアが 60 点未満（2 回 rewrite 後も）:**
```
⚠️ 品質スコアが基準に達していません。
問題箇所: {スウィープで検出された項目}
ユーザーの判断を仰ぎます。
- そのまま保存して人手で修正
- 再度 rewrite を試行
- 対象記事を変えて再実行
```

---

## 完了後の案内

完了サマリーの表示のみ行う。追加のコメントは不要。
Brain 投稿完了後、ユーザーが Notion レコード更新 + Phase 8 の PDCA 記録を行う。
