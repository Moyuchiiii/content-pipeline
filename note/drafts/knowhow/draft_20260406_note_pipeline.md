---
title: 自分のnote記事はほぼClaude Codeが書いてる。その仕組みを全部見せる話
type: knowhow
price: 0
tags: [ClaudeCode, note自動化, SKILL.md, Notion, コンテンツ自動化, 副業, 大学生, AI, パイプライン, コンテンツ制作]
created: 2026-04-06
status: final
score: 22
---

# 自分のnote記事はほぼClaude Codeが書いてる。その仕組みを全部見せる話

---

（冒頭アイランド）
**ネタ出し・リサーチ・執筆・サムネ作成、毎回ゼロからやっていますか？**

---

このnote記事、Claude Codeが書いた。

正確には「/note-run」と打ってGoを出したら、ネタ探しからリサーチ・HTML生成・Notionへの登録まで全部勝手に終わった。自分がやったのはトピックの確認と最後のnoteへのコピペだけ。

「その結果どうなったか」はひとつ前の記事に書いた。今回は「どうやって作ったか」を全部見せる。SKILL.mdの設計思想、Notionの2DB構造、9フェーズの処理順序——ぜんぶ実物ベースで解説する。

## 仕組みの全体像

核心は3つ。Claude CodeのSKILL.md、Notionの2DB設計、そして9フェーズの処理順序。これを組み合わせた自作パイプラインが「content-pipeline」という名前でローカルで動いてる。

**トレンドスキャン → トピック選定 → 競合調査 → 執筆 → 品質チェック → 保存 → Notion更新**

このフローをSKILL.mdに書いておくと、「/note-run」を打つだけでClaude Codeが上から順に実行してくれる。1コマンドで完結する仕組みはここから来てる。

## SKILL.mdは「AIへの仕事の説明書」

Claude CodeのSKILL.mdは、スラッシュコマンドが何をすべきかを書いたMarkdownファイル。「/note-run」を打つとClaude CodeはこのSKILL.mdを読み込んで作業を始める。

最初は「note記事を書いて。文体はこのサンプルに合わせて」みたいな1行プロンプトで試してた。結果は毎回ブレブレで、どこかで見たようなAI文章しか出てこなかった。

変わったのは「考える順番」を書いたとき。「最初にパフォーマンスデータを読む → 次にトレンドをスキャンする → その後でスコアリングする」という処理の流れをSKILL.mdに明文化した。それだけで出力の一貫性が段違いになった。

> **SKILL.mdに書くべきもの**
> このコマンドの目的と完了条件
> 各フェーズで「何を・どの順番で・どのファイルを参照して」実行するか
> 出力の形式と保存先
> エラー時の対処方法

「細かく書きすぎでは？」と思うくらいが正しい。曖昧さを残すとAIは最もラクな選択肢、つまり汎用的で個性のない内容を選ぶ。指示を具体化するほど、出力が人間らしくなる。

## 9フェーズの設計と、処理順序が重要な理由

/note-runは9つのフェーズで動く。順番には意図があって、「先に過去データを読まないとスコアリングが機能しない」「先にリサーチしないと執筆がブレる」という依存関係がある。

> **Phase 0**
> 過去記事のパフォーマンスデータを読み込み、「何が刺さったか」を自動分析する

> **Phase 1〜2**
> WebSearchでトレンドスキャン → 5項目スコアリングでトピック自動選定

> **Phase 3〜4**
> note.comで競合調査 → タイトル5候補生成 → スコアリングで最適タイトル選択

> **Phase 5〜6**
> HTML形式で執筆 → 7スウィープ編集 → 100点ルーブリックで品質チェック → 事実確認スウィープ

> **Phase 7〜9**
> SEO最適化・Canva MCPでサムネ自動生成・Notionに記録・PDCAフィードバック更新

## Notionが「自動学習する編集部」になる

2つのDBを使ってる。ネタ帳と記事管理DB。

ネタ帳はスキャンで見つかったトピック候補を「未使用/採用済み/見送り」で管理するもの。見送った理由も記録するので、同じネタを次回また候補に上げることがなくなる。

記事管理DBは公開後のPV・スキ数・転換率を記録して、Phase 0の「過去データ分析」に使う。このループが回ると、記事のトピック選定が徐々に精度を上げていく。

## 実際につまずいた2つのこと

1つ目はコンテキスト枯渇。SKILL.mdを1セッションで全実行しようとするとPhase 6あたりで文脈保持が限界に来る。解決策は/compactコマンドを中間に挟むことと、CLAUDE.mdに「セッション再開時に引き継ぐべき情報」を書いておくこと。

2つ目は事実確認の甘さ。WebSearchで拾った情報を鵜呑みにして、存在しない機能について書いてしまったことがある。今のPhase 6には「記事内のすべてのクレームをWebFetchで一次情報確認する」というステップがある。

## Before / After

**導入前:** 週5時間。ネタ探し1h・リサーチ1h・執筆2h・サムネ+SEO1h

**導入後:** 週40分。トピック確認5分・コマンド実行して待機30分・noteにペースト5分

削減率87%。最近のスキ転換率は20〜40%台で安定してきてる。

## まとめと次回予告

SKILL.mdの設計思想は「AIへの仕事の説明書を書く」こと。NotionのDB設計は「過去データを読み込んでスコアリングに反映させる学習ループ」を作るためにある。

次回はnote記事からXスレッドへの自動変換の仕組みを書く予定。x-runスキルの設計もcontent-pipelineの一部で、noteと同じフレームワークで動いてる。

---

[有料記事CTA]
SKILL.mdをさらに進化させた「副業ワークフローの自作プラグイン化」もまとめてある。
▶ 副業ワークフローをClaude Codeプラグインに詰め込んだら、端末が変わっても5秒で再開できた話（¥500）
https://note.com/moyuchi_aistu/n/nfdc5f1dac0ce

[あわせて読みたい]
- 【Claude Code】ショート台本を自動作成するAIツールの作り方｜コード不要
  https://note.com/moyuchi_aistu/n/nb5bffb185a04
- Claude Codeの/loopで副業タスクを放置してみたら、朝には終わってた話
  https://note.com/moyuchi_aistu/n/n62d37bf48e7b
